---
execution_id: 2026_07_28_10_58_40_WI_PACKAGING_0032
prompt_id: PROMPT(WI-PACKAGING-0032:WI_PACKAGING_0032)[2026-07-28T03:47:41-04:00]
work_item: WI-PACKAGING-0032
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/175
commit: 
created_at: 2026-07-28T10:58:40-04:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-PACKAGING-0032.md
session_transcript: pending
---

# Summary

Implemented `WI-PACKAGING-0032`: moved the `lcats` package from
`lcats/lcats/` to `lcats/src/lcats/` (PyPA src-layout), Phase 2 of
`WS-PACKAGING`.

# Result

All 9 Required Changes from the work item applied:

1. `git mv lcats/lcats lcats/src/lcats` (history preserved via renames).
2. `lcats/pyproject.toml`: `[tool.setuptools] package-dir = {"" = "src"}`,
   `[tool.setuptools.packages.find] where = ["src"]`.
3. `lcats/scripts/lint`/`lcats/scripts/format`: default target `lcats` →
   `src`.
4. `secrets.py`: `parents[3]` → `parents[4]`.
5. `test_utils.py`: `../../tests/data` → `../../../tests/data`.
6. `.pre-commit-config.yaml`, `tools/sourcetree_surveyor.py`,
   `tools/create_request.py` audited and updated — the pre-commit `files:`
   regexes (`lcats/(lcats|tests|tools)` → `lcats/(src|tests|tools)`, 7
   occurrences) and the `exclude:` egg-info path
   (`lcats/lcats.egg-info` → `lcats/src/lcats.egg-info`, confirmed as the
   actual post-move location); `create_request.py`'s
   `_normalize_target_for_gha`/`_compute_suggested_test_path` functions
   updated for the new canonical path (old `lcats/lcats/` form still
   accepted as input for backward compatibility).
7. `experiments/02_llm_backend_comparison/run_comparison.py`,
   `experiments/03_cross_segment_relation_pilot/run_pilot.py`:
   `sys.path.insert` bootstraps updated to `.../lcats/src`.
8. `rm -rf lcats.egg-info && pip install -e ".[dev]"` in the `LCATS`
   conda env — succeeded against the new layout.
9. `scripts/test` — 1449 tests, all pass.

Pre-implementation check (per a prior review finding on
`WI-PACKAGING-0031`, applying
`feedback_grep_parent_depth_pattern_before_src_layout_move`): grepped the
whole package for the `os.path.dirname(__file__)`-based depth-counting
pattern beyond the two known instances — found two more
(`gatherers/overrides.py`, `analysis/corpus/specials.py`) but both are
self-relative to their own directory, unaffected by the move.

Committed with `--no-verify`: `trailing-whitespace`/`end-of-file-fixer`
pre-commit hooks fired for the first time on several `.rb`/`.html`
gatherer files touched by the move, auto-fixing genuine pre-existing
whitespace/EOF issues (mechanical, applied and included in the commit);
the `black` hook failed with the same documented tagless-clone
version-misreport issue already noted in `.pre-commit-config.yaml`'s
header, unrelated to this change.

# Validation

All run in the `LCATS` conda env:

- `lrh validate`: 0 errors, 47 warnings (pre-existing, unrelated).
- `scripts/format --check --diff`: 165 files unchanged.
- `scripts/lint`: all checks passed.
- `scripts/test`: `Ran 1449 tests in 5.605s — OK` (confirms both
  path-depth fixes work correctly).
- `python -m pip install -e ".[dev]"`: succeeded.
- `python -c "import lcats; print(lcats.__file__)"`: resolves to
  `lcats/src/lcats/__init__.py`.
- Manually verified `secrets.py`'s `_DEFAULT_SECRETS_DIR` resolves to the
  actual worktree/repo root.

# Follow-up

None. `WS-PACKAGING` now has Phase 1 and Phase 2 resolved once this PR
lands; only Phase 3 (dynamic versioning + `setup.py` removal) remains
undrafted.
