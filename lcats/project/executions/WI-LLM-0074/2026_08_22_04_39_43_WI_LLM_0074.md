---
execution_id: 2026_08_22_04_39_43_WI_LLM_0074
prompt_id: PROMPT(WI-LLM-0074:WI_LLM_0074)[2026-08-22T04:09:51+00:00]
work_item: WI-LLM-0074
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/349
commit: e04a717112ac638b4c28b6849ae1201169742b09
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0074.md
session_transcript: claude-app:b0d48070-0faf-4a35-942d-a29ec96d603a
created_at: 2026-08-22T04:39:43+00:00
---

# Summary

`/lrh-execute WI-LLM-0074` (via inlined `/lrh-implement`): wire
`experiments/05_metadata_genre_prefilter/run_prefilter.py`'s `--validate`
mode to an opt-in local OpenAI-compatible backend (`--backend`/
`--base-url`), mirroring `run_census.py`'s own `WI-LLM-0066` wiring, so a
local model can later be validated against the exact same 146-story
sample the real Opus validation (`WI-GENRE-0004`) already used.

Per the WI's own Non-Goals, this PR builds the wiring only - no real
local-model run happens or is committed in this pass; `WI-LLM-0074`
itself is expected to stay `proposed` after this PR merges, pending a
separate, explicitly-gated follow-up run.

# Result

Added `--backend`/`--base-url` flags, `_build_validation_backend()`,
`_validation_output_filenames()` (model/endpoint-qualified, never
colliding with the existing Opus evidence files), and extended
`_validation_fingerprint()` to include the effective backend/endpoint
(bumping `VALIDATION_FINGERPRINT_VERSION` v1->v2 to invalidate stale
checkpoints). `total_estimated_cost_usd` is `$0` for any local endpoint,
both in the pre-run estimate and the real-run summary. 8 new tests;
README documents the new flags and the deferred-real-run scope
boundary. Landed via PR #349.

A diff-mode `/lrh-self-review` pass (before this PR's first push) found
no blocking issues across all six acceptance criteria; independently
re-verified its one side-claim (2 pre-existing `scripts/test` failures)
and found it did not hold up - `scripts/test` reproduced 1830 tests/OK/
exit 0 twice, with zero occurrences of `FAILED` or the cited test names
in either log.

# Validation

- `PYTHONPATH=lcats/src python -m pytest experiments/05_metadata_genre_prefilter/run_prefilter_test.py`: 53 passed (45 prior + 8 new).
- `scripts/test` (full repo suite): 1830 tests, OK - run twice independently.
- `scripts/format --check --diff` / `scripts/lint`: clean.
- `lrh validate`: 0 errors, 168 pre-existing warnings (unrelated).
- Manual verification against the real committed 146-story manifest:
  default-backend estimate unchanged (`$34.008505`, byte-identical to
  before this change); `--backend openai --base-url
  http://localhost:11434/v1 --model gpt-oss:20b` correctly estimates
  `$0`.

# Follow-up

- `WI-LLM-0074` will remain `proposed` after this PR merges - its
  acceptance criteria requiring the real 146-story local-model run's
  measured results (three-way comparison, go/no-go recommendation)
  can't be satisfied until that separately-gated run happens, mirroring
  `WI-GENRE-0004`'s own two-PR split (wiring, then real evidence).
