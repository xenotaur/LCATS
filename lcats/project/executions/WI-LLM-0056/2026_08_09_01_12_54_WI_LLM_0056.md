---
execution_id: 2026_08_09_01_12_54_WI_LLM_0056
prompt_id: PROMPT(WI-LLM-0056:WI_LLM_0056)[2026-08-08T19:35:58+00:00]
work_item: WI-LLM-0056
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/270
commit: fb9172803f0dd5f953f2bed5ac85984c3493ded9
created_at: 2026-08-09T01:12:54+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0056.md
session_transcript: claude-app:6d988910-ee4a-4ccc-af0b-2fb13d91ddc5
---

# Summary

Implement WI-LLM-0056 (tranche 1 cross-provider coverage) via
`/lrh-execute` - land one real, working candidate per provider/online-
offline cell (Anthropic 2nd tier, OpenAI online, OpenAI offline, Gemini
online, Gemma offline, second open-weight family offline) in
`lcats/experimental/model_comparison/`.

# Result

**Partial landing - 4 of 6 cells landed with real evidence, 2 pending on
network access, PR #270 not yet merged.**

Verified every model ID live against each provider's own API/registry
before use (not trusted from web-search-era research, per the WI's own
Risk Notes) - `claude-haiku-4-5` via `client.models.list()`, current
OpenAI model family via `client.models.list()` (chose `gpt-5.5`, a
stable non-preview tier), Gemini's real model catalog via its
OpenAI-compat `models.list()` (chose `gemini-3.5-flash`), and Ollama's
public registry API for `gpt-oss:20b`/`gemma4:12b`/`deepseek-r1:14b`
tag existence and download size.

**Landed:**
- `anthropic_haiku` (claude-haiku-4-5): 1/1 success, 27.9s, 20 entities -
  matched `anthropic_opus`'s entity count (21) at roughly half the
  latency/output tokens.
- `openai_gpt55` (gpt-5.5): first attempt blocked on account credits
  (HTTP 429); after the user added credits, a second real attempt
  surfaced a genuine bug in the shared production `ENTITY_TOOL_SCHEMA`
  (`entity_extractor.py:66-68`) - `grammatical_role` is a declared
  property missing from its own `required` array, which OpenAI's strict
  function-calling validator correctly rejects (`invalid_function_parameters`)
  and Anthropic's strict-tool-use validation does not enforce. Documented
  precisely with exact file:line and traced root cause; not fixed in this
  PR since it's a production schema bug affecting every real caller of
  `make_entity_extractor()`, not an `lcats.llm` backend issue or something
  scoped to this benchmark harness - flagged as a follow-up.
- `ollama_gpt_oss_20b` (gpt-oss:20b): 2/2 success, 35-38s, one run matched
  `anthropic_opus`'s exact entity count (21).
- `gemini_flash` (gemini-3.5-flash): 2/2 failed, `finish_reason:
  'function_call_filter: MALFORMED_FUNCTION_CALL'` - Gemini's own
  compat-layer filter rejects `ENTITY_TOOL_SCHEMA`'s tool call as
  malformed before it reaches this harness. Ran a live diagnostic testing
  whether `strict: true` was the trigger (disabled it, full segment) -
  identical failure recurred, ruling that out; root cause left
  uncharacterized further (out of this tranche's scope), documented as a
  real, reproducible negative finding.

**Pending (real code, no results yet - network-blocked, not code or
hardware issues):**
- `ollama_gemma4_12b` (gemma4:12b, 7.6GB): pull failed at 94% with a
  network error partway through this session (laptop shutdown/cafe wifi).
- `ollama_deepseek_r1_14b` (deepseek-r1:14b, 9.0GB): pull never completed
  in this session's network conditions.
- Both have complete, correct `setup.py`/`benchmark.py`/`README.md`
  (the README explicitly states "pending" rather than showing fabricated
  or placeholder results) - re-running `ollama pull <model>` then
  `python benchmark.py` on reliable network is all that's needed to
  complete them.

**Real substitution documented:** the WI's own research named DeepSeek V4
and GLM-5.2 as the second-open-weight-family candidates, but live
verification against Ollama's registry found both are cloud-only (no
locally-runnable tag exists) - `deepseek-r1:14b` substituted as the real,
locally-runnable DeepSeek-lineage alternative, with the verification
documented in `ollama_deepseek_r1_14b/README.md`'s "Research context"
section.

`WI-LLM-0056` is deliberately **not** marked resolved by this record -
its own acceptance criteria require all 6 cells to land or be explicitly
deferred with a documented reason, and a temporary network outage isn't
the same as the "doesn't fit available hardware" reason the WI's
acceptance criteria names. This is real, honest partial progress; the WI
stays `proposed` (with this execution record showing `in_progress`)
pending a follow-up run to complete the last 2 cells.

# Validation

- `pip install -e .` - re-fixed a stale editable install pointing at a
  different worktree (recurring environment issue this session).
- `python -m pytest tests/llm_tests -q` - 44 passed.
- `black --check --diff` / `ruff check` on `experimental/model_comparison/`
  (CI-pinned versions, re-pinned after ambient drift) - clean.
- `lrh validate` - 0 errors, pre-existing warnings only.
- Real, live API/local-model calls for every landed candidate: 1 call
  (`anthropic_haiku`), 2 calls (`openai_gpt55` - one blocked, one real),
  2 calls (`ollama_gpt_oss_20b`), 2 calls + 1 diagnostic
  (`gemini_flash`) - all committed/documented regardless of outcome,
  including genuine failures.
- `.secrets/gemini-api-key.env` created and verified to load correctly
  (`GEMINI_API_KEY` distinct from `OPENAI_API_KEY` to avoid a collision
  in the shared `os.environ` namespace both `.env` files load into) -
  copied into this worktree per the known cross-worktree `.secrets/` gap.

# Follow-up

- Complete `ollama_gemma4_12b` and `ollama_deepseek_r1_14b` once reliable
  network is available - pull the models, run `benchmark.py`, commit real
  `results.json`, then resolve `WI-LLM-0056` for real.
- File (or flag) a follow-up for the `ENTITY_TOOL_SCHEMA` `grammatical_role`
  `required`-array bug in `entity_extractor.py:66-68` - a real production
  correctness issue affecting every real caller, not just this benchmark.
- The Gemini `MALFORMED_FUNCTION_CALL` root cause (which specific schema
  property triggers it) remains uncharacterized - would need testing
  simplified/intermediate schema shapes, out of this tranche's scope.
