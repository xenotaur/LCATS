---
execution_id: 2026_08_20_00_41_19_WI_SEGMENT_0069
prompt_id: PROMPT(WI-SEGMENT-0069:WI_SEGMENT_0069)[2026-08-19T22:39:05+00:00]
work_item: WI-SEGMENT-0069
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/320
commit: f38878c0bcb19c5eb1ad032049ca89a56dad1a29
created_at: 2026-08-20T00:41:19+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-SEGMENT-0069.md
session_transcript: claude-app:693d6013-727b-422d-a378-5dc4242d3076
---

# Summary

Implemented `WI-SEGMENT-0069`: classified the segmentation alignment
failures that remain after `WI-SEGMENT-0068`'s whitespace-mismatch fix,
with real evidence and a per-category recommendation.

# Result

- Required Change 1: `check_segmentation_reliability.py` now persists
  `parsed_output` (the raw, pre-alignment segments dict) alongside every
  result, so an `alignment_error` outcome can be analyzed after the fact
  without a fresh live LLM call (`extracted_output` is cleared to `None`
  on that path by `JSONPromptExtractor.extract()`'s own design,
  `WI-SEGMENT-0059`).
- Required Change 2: ran a live 30-story smoke test
  (`claude-haiku-4-5-20251001`, real API cost, sample size explicitly
  authorized in-session) and classified all 21 `alignment_error` failures
  into three named categories with real counts:
  `anchor_absent_from_document` (15/21), `paragraph_misnumbering_large_margin`
  (4/21), `paragraph_misnumbering_narrow_margin` (2/21).
- Required Changes 3-4: no correlation found between paragraph
  mis-numbering and story paragraph count (a real negative result).
  Manual inspection of the 15 anchor-absent cases found two concrete,
  narrowly-fixable sub-patterns -- paragraph-index marker leakage into
  anchor text (3 stories directly, affecting many more segments within
  them per `parsed_output`'s full segment list), and Unicode
  curly-quote/dash typography mismatch (2 stories) -- accounting for
  5/21 failures, plus a residual near-miss-quoting bucket (10/21) with
  no safe targeted fix identified.
- Required Change 5: findings, evidence, and recommendations recorded as
  `lcats/project/design/segmentation-alignment-failure-categories.md`,
  following `WI-EVENT-0028`'s investigation-type precedent.
- Added `classify_alignment_failures.py`, implementing the reproducible
  classification method against any `check_segmentation_reliability.py
  --output` directory.
- Opened [PR #320](https://github.com/xenotaur/LCATS/pull/320).

# Validation

- `python -m pytest experiments/03_cross_segment_relation_pilot/check_segmentation_reliability_test.py experiments/03_cross_segment_relation_pilot/classify_alignment_failures_test.py` -- 15 passed (13 at initial implementation; 2 more added while fixing review findings -- see below)
- `./scripts/test` (full suite) -- 1762 passed
- `black --check` / `ruff check` on all changed/new files -- clean
- `lrh validate` -- 0 errors, 151 pre-existing warnings (unrelated)
- PR #320 review (Codex + Copilot, first round): 4 real findings, all fixed
  -- `classify_alignment_failures.py`'s `classify_story()` now mirrors
  `align_segment`'s exact sequential anchor-resolution order (with a
  regression test); the design doc's mis-numbering table now cites
  concrete claimed ranges and real anchor positions per story; the
  "correlation ruled out" language was softened to match what a 6-vs-6
  sample can actually support; `classify_story()` now degrades to a
  named diagnostic category instead of crashing on an unreadable
  `story.json` (with a regression test).
- Independent PR-mode self-review (`/lrh-self-review`, cold subagent):
  verified `classify_story()` genuinely mirrors `align_segment` line for
  line, recomputed all 6 mis-numbering table rows against the code's own
  margin formula, confirmed the `parsed_output`/`extracted_output`
  contract against `llm_extractor.py` directly, and re-ran the test
  suite (15 passed). No functional or factual defects found.

# Follow-up

- This record documents implementation; `status` stays `in_progress`
  until `/lrh-land` merges PR #320 and lands closeout.
- The "fix now" category (paragraph-marker stripping + typography
  normalization in `_locate_anchor_span`) is scoped as a follow-up
  deliverable WI, not implemented here (this is an investigation-type
  work item per its `type`).
