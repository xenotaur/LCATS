---
execution_id: 2026_08_08_02_46_10_WI_LLM_0051
prompt_id: PROMPT(WI-LLM-0051:WI_LLM_0051)[2026-08-08T00:23:59+00:00]
work_item: WI-LLM-0051
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/254
commit: b51e1443dd18c553373f50cab1a5f4eb6ce778ee
created_at: 2026-08-08T02:46:10+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0051.md
session_transcript: claude-app:6d988910-ee4a-4ccc-af0b-2fb13d91ddc5
---

# Summary

Implement WI-LLM-0051 via `/lrh-execute` (which inlines `/lrh-implement`
then `/lrh-land`) - investigate whether Ollama's OpenAI-compatible
`tool_choice` forced-function-name support has a real, reproducible gap,
building on `WI-LLM-0050`'s existing 2/2 segmentation-stage failures
against `ollama_qwen3_8b` as primary evidence rather than re-deriving
from scratch.

# Result

Fixed a pre-existing bug in `WI-LLM-0051.md`'s own frontmatter before
implementation could even start: comments interleaved inside the
`artifacts_expected` YAML list are valid YAML (PyYAML parses them fine)
but broke lrh's own hand-rolled frontmatter parser
(`lrh.control.parser.parse_markdown_text`), making
`lrh work-items readiness WI-LLM-0051` fail with "work item not found"
even though `lrh validate` reported 0 errors - only `lrh work-items
validate` caught it as `malformed-frontmatter`. Removed the interleaved
comments (their content was already present in the Scope section prose,
so nothing was lost). Flagged the identical bug in `WI-LLM-0056.md` as a
separate background follow-up task rather than fixing it in this PR
(out of scope).

Ran 3 more real, live Ollama benchmark calls via `common/harness.py`'s
`run_segmentation()`: a 3rd attempt at the identical `(qwen3:8b,
five_orange_pips)` config `WI-LLM-0050` used, a different story
(`corpora/sherlock/engineers_thumb`) on the same model, and a different
model (`qwen3:30b-a3b`) on the same story. All 3 failed identically
(`finish_reason='stop'`, no tool call, despite schema-shaped free-text
content) - **0/5 total segmentation attempts succeeded** across 2 models
and 2 stories, including 3 independent samples at the identical config -
a systemic gap, not intermittent noise. `run_entity_extraction()`'s
smaller/flatter schema has never shown this failure across any candidate
tested in this proposal's history, consistent with a schema-complexity
explanation.

**Draft conclusion, self-corrected during review-response:** the first
implementation pass concluded "retry has no observed chance of
succeeding, explicitly rejected" - inferred solely from the 3
identical-config repeats never differing. The automatic first-push
review (Codex) correctly identified this as unsound: repeating the
identical request only tests `temperature=0.6` resampling, not the WI's
own named mitigation (an explicit reminder appended to the system
prompt). Rather than defend or merely soften the original claim, tested
the actual named mitigation directly: 5 live calls at the identical
config with `"CRITICAL INSTRUCTION: You MUST call the record_segments
function/tool ..."` appended to the system prompt. **2/5 succeeded
(40%)** - a real, substantial improvement over the 0/5 baseline, though
still far from reliable.

**Result: implemented, not rejected.** `common/harness.py`'s
`run_segmentation()` now retries exactly once, automatically, when the
first attempt fails with `error_type="no_tool_call"` specifically -
appending the tested reminder to the system prompt on the retry only.
Any other failure mode (genuine `api_error`, schema/validation error,
empty segment list) is not retried. Verified end-to-end with one more
real live call: first attempt failed (`no_tool_call`), automatic retry
succeeded, final result `success=True, retry_attempted=True,
retry_succeeded=True`, 4 segments extracted -
`ollama_qwen3_8b/results_segmentation.json` (this candidate's canonical
"latest run" file) now reflects that call. The pre-retry-code baseline
failures remain preserved separately in `results_segmentation_run1-3.json`.

Also committed real, runnable evidence for the two varied-condition data
points (a second Codex finding on the same PR): added
`ollama_qwen3_8b/benchmark_segmentation_engineers_thumb.py` and
`ollama_qwen3_30b_a3b/benchmark_segmentation.py`, re-ran both for real
(`retry_with_reminder=False`, to keep the baseline-frequency evidence
uncontaminated by the retry mechanism), committed their `results*.json`
outputs - not prose-only claims as in the initial draft.

Rewrote `PROP-ERW-LOCAL-MODEL-EVALUATION`'s "Decision 3 update
(2026-08-08 ...)" section, Implementation Plan item, and Open Questions
to reflect the corrected verdict, and updated both
`ollama_qwen3_8b/README.md` and `ollama_qwen3_30b_a3b/README.md`. Also
fixed a pre-existing bug in `WI-LLM-0051.md`'s own frontmatter before
implementation could even start: comments interleaved inside the
`artifacts_expected` YAML list are valid YAML (PyYAML parses them fine)
but broke lrh's own hand-rolled frontmatter parser
(`lrh.control.parser.parse_markdown_text`), making
`lrh work-items readiness WI-LLM-0051` fail with "work item not found"
even though `lrh validate` reported 0 errors - only `lrh work-items
validate` caught it. Removed the interleaved comments (their content was
already present in the Scope section prose, so nothing was lost).
Flagged the identical bug in `WI-LLM-0056.md` as a separate background
follow-up task rather than fixing it in this PR (out of scope). Also
flagged a follow-up idea: testing whether the same reminder would help
the real production `SCENE_SEQUEL_SYSTEM_PROMPT` in `scene_analysis.py`
(out of this WI's Non-Goals - shared backend/production prompt changes).

Ran a diff-mode `/lrh-self-review` pass before the PR's first push and a
PR-mode pass before merge (cold-context subagents, per this session's
standing guidance to use self-review rather than manually retriggering
GitHub bots). Two false-positive Copilot findings on the first-push
review (claiming committed results JSON was invalid) were independently
verified false via 3 methods (python `json`, `jq`, re-fetching the exact
remote blob) and skipped with documented rationale, not blindly applied.
The PR-mode pass caught one more real issue: the PR's own top-level
description still contained the superseded "retry rejected" language
after the fix commit - corrected via `gh pr edit --body-file` (a plain
`gh pr edit --body "$(cat <<'EOF' ...)"` heredoc silently mangled the
content, backticks getting interpreted as real command substitution
rather than literal text - a new failure mode of a previously-known
heredoc pitfall, saved to memory).

# Validation

- `pip install -q "ruff==0.15.0" "black==25.11.0"` - re-pinned multiple
  times after ambient drift (recurring environment issue this session).
- `python -m pytest -q` (full suite, after the retry-path code change) -
  1659 passed.
- `black --check --diff` / `ruff check` on `experimental/model_comparison/`
  (CI-pinned versions) - clean.
- `lrh validate` - 0 errors, pre-existing warnings only.
- `lrh work-items validate` - `WI-LLM-0051.md` no longer flagged
  malformed (1 remaining error is the separately-flagged `WI-LLM-0056.md`
  bug, out of this PR's scope).
- 6 real `run_segmentation()`/reminder-test calls (not simulated)
  against a live local Ollama server across the two commits, all
  committed/documented as evidence regardless of outcome, including the
  retry path validated end-to-end with a real success.

# Follow-up

- `WI-LLM-0056.md` has the identical frontmatter-parser bug - flagged as
  a separate background task, not fixed in this PR.
- Whether the same reminder would help the real production
  `SCENE_SEQUEL_SYSTEM_PROMPT` (not just the benchmark harness) - flagged
  as a separate background task, not investigated in this PR.
- The underlying question of *which* schema property (size, nesting
  depth, something else) triggers the base `tool_choice` gap, and why
  the reminder only helps 40% of the time rather than reliably, remain
  open - would require testing intermediate schema shapes, out of this
  investigation's scope.
