---
execution_id: 2026_08_10_05_00_57_WI_LLM_0064_IMPL
prompt_id: PROMPT(WI-LLM-0064:WI_LLM_0064)[2026-08-10T04:28:22+00:00]
work_item: WI-LLM-0064
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/281
commit: 2897d982b9bf5884db04513b9f5a458ee29e21c2
created_at: 2026-08-10T05:00:57+00:00
agent: codex_app
instruction_source: project/work_items/proposed/WI-LLM-0064.md
session_transcript: none
---

# Summary

Implement `WI-LLM-0064` via LRH: test the already-installed local
`gpt-oss:20b` candidate under a fairer best-config setup, add diagnostic
capture that the prior harness lacked, and update the model-comparison
recommendation from the grounded evidence.

# Result

- Confirmed local `gpt-oss:20b` before implementation (`ollama list`,
  model id `17052f91a42e`; `ollama show gpt-oss:20b --parameters`
  reported bundled `temperature 1`).
- Added additive diagnostic helpers to
  `experimental/model_comparison/common/harness.py`:
  `run_segmentation_diagnostic()` preserves parsed pre-alignment segment
  anchors and exact-substring checks, and
  `run_entity_extraction_with_grounding()` reports raw tool-result entity
  counts beside production `build_entities()` grounded counts.
- Added `ollama_gpt_oss_20b/benchmark_entity_bestconfig.py` and
  `benchmark_segmentation_bestconfig.py`, both using the local Ollama
  OpenAI-compatible endpoint at `temperature=1.0`.
- Ran and committed the live benchmark JSON evidence:
  - Entity extraction: raw tool-call success 3/3, but grounded entity
    success 0/3 because the model emitted `mentions` as plain strings
    rather than production mention objects.
  - Segmentation at `temperature=1.0`: 0/3, all `no_tool_call` with
    schema-shaped JSON in message content.
  - Segmentation at `temperature=1.0` plus verbatim-anchor reminder: 0/3;
    two runs reached tool calls but failed anchor alignment, and one
    returned no tool call/refusal.
- Updated `ollama_gpt_oss_20b/README.md` and
  `PROP-ERW-LOCAL-MODEL-EVALUATION` with the revised recommendation:
  prefer `gpt-oss:20b` only for genre detection; consider entity
  extraction only as a follow-up prompt/schema/output-handling target;
  do not consider segmentation under the current Ollama
  OpenAI-compatible path.
- Opened draft PR #281:
  https://github.com/xenotaur/LCATS/pull/281

# Validation

- `conda run -n LCATS python experimental/model_comparison/ollama_gpt_oss_20b/benchmark_entity_bestconfig.py`
- `conda run -n LCATS python experimental/model_comparison/ollama_gpt_oss_20b/benchmark_segmentation_bestconfig.py`
- `conda run -n LCATS python -m unittest tests.analysis_tests.model_comparison_harness_test`
- `scripts/test` - 1705 tests OK.
- `lrh validate` - 0 errors, pre-existing repo warnings only.
- `scripts/format --check --diff` using temporary shims to the LCATS
  conda env's Black 25.11.0, avoiding the known Homebrew Black drift.
- `scripts/lint` using temporary shims to the LCATS conda env's Ruff
  0.15.0 and Black 25.11.0.
- `conda run -n LCATS python -m black --check --diff` over the modified
  experimental Python files, which the repo `scripts/format` target does
  not cover.
- Manual diff/self-review pass before PR creation: checked that the
  harness changes are additive, that existing `run_*` contracts/defaults
  remain unchanged, and that the README/proposal numbers match the
  committed result JSON.

# Follow-up

- Investigate whether `gpt-oss:20b` entity extraction can be made
  production-grounded with a schema-specific prompt reminder, stricter
  output validation, or a small compatibility adapter without weakening
  production semantics.
- Segmentation remains not viable for this candidate under the tested
  OpenAI-compatible Ollama path; native Ollama `/api/chat` remains a
  broader, still-untested local-runtime follow-up from earlier WIs.
- `session_transcript: pending` should be updated if/when this Codex
  desktop task receives a durable transcript identifier.
