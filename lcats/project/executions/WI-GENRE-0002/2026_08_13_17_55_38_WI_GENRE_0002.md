---
execution_id: 2026_08_13_17_55_38_WI_GENRE_0002
prompt_id: PROMPT(WI-GENRE-0002:WI_GENRE_0002)[2026-08-13T16:00:09+00:00]
work_item: WI-GENRE-0002
status: in_progress
rerun_of:
pr: https://github.com/xenotaur/LCATS/pull/301
commit:
created_at: 2026-08-13T17:55:38+00:00
agent: codex_app
instruction_source: project/work_items/proposed/WI-GENRE-0002.md
session_transcript: codex-app:019ff36e-af10-7da3-9222-02c0a2bee6a4
---

# Summary

Implement `WI-GENRE-0002`: extend `experiments/05_metadata_genre_prefilter` from a no-network dry-run scaffold into an experiment-local Gutenberg metadata-rule evidence pilot and deterministic 40-story pilot manifest generator.

# Result

- Updated `experiments/05_metadata_genre_prefilter/run_prefilter.py` to:
  - keep LCATS story ID as the corpus-root-relative story bucket path;
  - report omitted, missing, invalid, or ready cache state without downloading or building a cache;
  - read subjects only from an explicitly supplied existing SQLite cache in read-only mode;
  - load `GENRE_RULES` from source without importing `lcats.utils.genre` or `lcats.gettenberg.cache`;
  - record all matching metadata labels and matched patterns;
  - normalize direct labels to the 8 LCATS target genres while preserving `Crime` as suggestive `mystery` evidence and non-target labels as secondary signals;
  - emit assessment-shaped metadata evidence with timestamp, method/version, provenance, `scope: "gutenberg_volume"`, raw subjects, raw rule matches, and result fields;
  - write exactly `candidates.jsonl`, `pilot_40_manifest.jsonl`, and `summary.json` under the requested output directory;
  - select a deterministic 40-story pilot across `lovecraft`, `sherlock`, O. Henry collections, and `mass_quantities`, with cap/shortfall/repeated-ID diagnostics.
- Updated experiment tests for cache schemas, read-only behavior, omitted-cache behavior, all-match rules, normalization, assessment shape, pilot selection, output guards, and no import of the mutating Gutenberg cache module.
- Updated the experiment README with cache sync expectations, output contract, evidence fields, normalization, and explicit non-goals.
- Ran diff-mode self-review before PR creation. Two P2 findings were fixed before opening PR #301: removed the extra `manifest.jsonl` output and changed omitted-cache behavior so no default cache can be read implicitly.

# Validation

- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/version tools` from `lcats/` - LCATS 0.1.1.dev528+gdedd1d187, Python 3.11.8, Ruff 0.15.0, Black 25.11.0.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/format --check --diff` from `lcats/` - 187 files unchanged.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/lint` from `lcats/` - Ruff and Black checks passed.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/test` from `lcats/` - Ran 1730 tests in 13.618s, OK.
- `lrh validate` from `lcats/` - 0 errors, 141 existing warnings.
- `python -m unittest experiments/05_metadata_genre_prefilter/run_prefilter_test.py` from the repository root - Ran 15 tests, OK.
- Targeted `black --check --diff` and `ruff check` over `experiments/05_metadata_genre_prefilter/run_prefilter.py` and `run_prefilter_test.py` - passed.
- No-cache smoke from the repository root - wrote exactly `candidates.jsonl`, `pilot_40_manifest.jsonl`, and `summary.json`; selected 40 pilot rows; reported `cache.status: "not_supplied"`.
- Explicit-cache smoke from the repository root using a temporary SQLite fixture under `/private/tmp` - reported `cache.status: "ready"`, populated `{"mystery": 12}` target counts and `{"Crime": 12}` secondary signal counts, and wrote exactly the three expected files.

# Follow-up

- Land PR #301 through review-response, confirm-fixes, SHA-locked merge, and closeout.
- Later work items should supply/review a real local Gutenberg cache path before any real-cache pilot output is considered for commit.
