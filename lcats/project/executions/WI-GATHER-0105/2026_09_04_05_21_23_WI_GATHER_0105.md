---
execution_id: 2026_09_04_05_21_23_WI_GATHER_0105
prompt_id: PROMPT(WI-GATHER-0105:WI_GATHER_0105)[2026-09-04T03:36:37+00:00]
work_item: WI-GATHER-0105
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/426
commit: 
created_at: 2026-09-04T05:21:23+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-GATHER-0105.md
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-execute WI-GATHER-0105` (inlined `/lrh-implement`) — added a
dedicated `RunLog` wrapper to `mass_quantities/gatherer.py`'s
`gather_stories()`, per `WI-GATHER-0101`'s audit's classification of
`mass_quantities` as structurally incompatible with `gatherlib.gather()`
reconciliation.

# Result

Before implementation began, `lrh work-items readiness WI-GATHER-0105`
reported `prompt_ready: no` (blocking: "missing Required Changes
section") — a document defect from an earlier review-response edit on
PR #419 that had accidentally dropped the `## Required Changes` heading,
leaving its numbered items orphaned directly under `## Non-Goals`.
Restored the heading before proceeding; re-ran readiness, confirmed
`prompt_ready: yes`.

Wrapped `gather_stories()`'s loop in a `run_log.RunLog` scope *inside
the function itself*, not around either of its two independent
production callers (`gather()`, `main()`), per the WI's own explicit
scoping requirement — emitting `run_start`, a per-story
`story_downloaded`/`story_failed` event, `run_end`, and
`run_aborted_unexpected`. Log destination pinned to
`logs/gather/mass_quantities_gather_run_log.jsonl` via a new
`GATHER_LOG_FILENAME` constant and an opt-in `log_dir` parameter
(default `logs/gather`, matching `gatherlib.gather()`'s own convention).

**Explicit recorded decision (acceptance criterion):** `parser.gather_
story()`'s existing narrow `api.load_etext()`-only per-story recovery
(`parser.py:1402-1405`) is preserved exactly unchanged — `parser.py`
was not touched by this diff at all (confirmed via `git diff origin/main
-- src/lcats/gatherers/parser.py`, empty).

Updated all 6 existing `TestGatherStories` tests to pass an explicit
`log_dir` pointing to a tempdir (avoiding real log-file leakage, per the
established pattern from `WI-GATHER-0103`/`WI-GATHER-0104`), and added a
new `TestGatherStoriesRunLogging` class (3 tests): clean-run event
ordering, a crash-mid-run partial-log test (confirms
`run_aborted_unexpected` and that events already written survive), and
a log-path/filename test.

Ran a diff-mode `/lrh-self-review` pass before pushing (Step 7.5) —
clean, no findings; the subagent independently confirmed `RunLog`
constructor usage, `parser.py`'s exact preservation, and traced every
`gather_story()` return path for the `story` variable's correctness. I
independently re-verified the `parser.py` preservation claim myself via
a direct empty-diff check.

Opened PR #426 (`xenotaur/feat/wi-gather-0105`) against `main`.

# Validation

- `lrh validate` — exit 0.
- Full test suite (`PYTHONPATH`-corrected) — 2260/2260 pass.
- `scripts/format --check --diff` / `scripts/lint` — clean.
- Smoke test confirms `logs/gather/mass_quantities_gather_run_log.jsonl`
  is produced by a real (non-mocked-loop) call.
- Diff-mode `/lrh-self-review` — clean, no findings; top claim
  independently re-verified.

# Follow-up

- Next: wait for reviewer comments,
  `/lrh-review-response`/`/lrh-confirm-fixes` on PR #426, then merge.
- This is the last open follow-up from `WI-GATHER-0101`'s audit — once
  this lands, all three sites (`sherlock`, `lovecraft`,
  `mass_quantities`) have uniform run-log coverage.
