---
execution_id: 2026_08_29_17_01_30_WI_PROMOTE_0101
prompt_id: PROMPT(WI-PROMOTE-0101:WI_PROMOTE_0101)[2026-08-29T16:44:24+00:00]
work_item: WI-PROMOTE-0101
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/416
commit: f0ce8bdc32a612f13f6b87abff8c6e237f335a3f
agent: claude_app
instruction_source: project/work_items/proposed/WI-PROMOTE-0101.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-29T17:01:30+00:00
---

# Summary

Implemented WI-PROMOTE-0101 (Stage 2 of `WS-PROMOTE-MODE-REDESIGN`):
added a targeted, registry-based orphaned-sidecar guard to `lcats
promote replace`.

# Result

- `src/lcats/analysis/corpus/promote.py`: added `OrphanedSidecarFinding`
  and extended `CollectionSurveyResult` with an
  `orphaned_sidecar_findings` field, updating `clean` to also require
  it empty. Added `_find_orphaned_sidecars(source_dir, dest_dir)`,
  which walks the *destination* collection's story buckets (via
  `discovery.iter_collection_story_files(dest_dir)` — the opposite
  traversal direction from every other check in this module) and
  flags any *registered* sidecar kind (via
  `sidecar_validators.registered_filenames()`) present at the
  destination for a story but missing from the corresponding source
  bucket. Returns `[]` immediately if the destination collection
  doesn't exist yet. Wired into `promote_collections()`'s per-collection
  loop: an otherwise-clean collection with orphan findings is rebuilt
  via `dataclasses.replace()` and added to `blocked`, unless the new
  `allow_orphaned_sidecar_deletion` parameter is True, in which case
  the check is skipped entirely.
- `src/lcats/analysis/corpus/promote_cli.py`: added
  `--allow-orphaned-sidecar-deletion` to `replace` only (absent from
  `insert`/`upsert`'s shared argument group), wired through, plus
  stderr reporting of orphaned findings per blocked collection.
- `tests/analysis_tests/promote_test.py`: added
  `OrphanedSidecarGuardTest` (guard fires by default, escape hatch,
  fresh-destination no-op, both-sides-present is not orphaned,
  registered-kinds-only scoping, insert/upsert unaffected) plus 3 new
  `PromoteCliTest` tests (CLI reporting, flag reaching
  `promote_collections`, flag is replace-only).
- `docs/reference/corpus-promotion.md`, `docs/reference/cli-commands.md`:
  documented the guard and the escape hatch.
- Diff-mode self-review (cold-context subagent) found no issues;
  independently re-verified the highest-risk claim (destination-vs-source
  traversal direction) directly before pushing.

# Validation

- `scripts/version tools`: ruff 0.15.0, black 25.11.0 (matches pins).
- `scripts/format --check --diff`: 2 files needed reformatting
  (`promote.py`, `promote_test.py`); applied via `scripts/format`,
  re-verified clean.
- `scripts/lint`: clean.
- `scripts/test`: 2247 tests, `OK`.
- `lrh validate`: 0 new errors.
- Real end-to-end smoke test (guard fires, escape hatch works, fresh
  destination unaffected) run manually against a temp corpus before
  writing tests.
- Diff-mode self-review: no findings; independently re-verified.

# Follow-up

- None. This was the last of `WS-PROMOTE-MODE-REDESIGN`'s three
  anticipated stages (Stage 1 `WI-PROMOTE-0097`, Stage 3
  `WI-PROMOTE-0100`, both already resolved).
