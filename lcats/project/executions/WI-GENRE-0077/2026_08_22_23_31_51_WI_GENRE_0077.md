---
execution_id: 2026_08_22_23_31_51_WI_GENRE_0077
prompt_id: PROMPT(WI-GENRE-0077:WI_GENRE_0077)[2026-08-22T20:30:03+00:00]
work_item: WI-GENRE-0077
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/362
commit: 8aeb659135111a9e638845fcb82c9c3b870b93ba
agent: claude_app
instruction_source: lcats/project/work_items/proposed/WI-GENRE-0077.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-22T23:31:51+00:00
---

# Summary

Implemented `WI-GENRE-0077`: promoted the real, already-validated
146-story `genre-sidecar-v1` evidence from `WI-GENRE-0004`'s gated Opus
validation run into `corpora/` as real `genre.json` files, using
`WI-GENRE-0075`'s `promote_sidecar_tranche()` tranche-promotion mode.
Per the requesting user's explicit instruction, this PR is intended for
broader-team review before merge, not an automatic self-merge.

# Result

- Ran `python3 -m lcats.analysis.corpus.promote_cli --tranche-manifest
  experiments/05_metadata_genre_prefilter/results/full_scan/validation_results.jsonl
  --dest corpora --dry-run` first: 146 would-promote, 0 rejected. Showed
  the exact count and a sample record (`anderson/bell`) to the user and
  got explicit, separate approval before running the real promotion, per
  this WI's own acceptance criteria and `forbidden_actions`
  (`promote_before_user_go_ahead`).
- Ran the real (non-dry-run) promotion: 146 promoted, 0 rejected,
  matching the dry-run exactly.
- Confirmed via `git diff --name-status` that all 146 changed paths in
  `corpora/` are pure additions (`A`) - zero modifications, zero
  deletions. No other file in any touched collection directory changed.
- Independently re-validated every one of the 146 promoted sidecars in
  its final `corpora/` location via `genre_sidecar.validate_sidecar()`:
  146/146 pass. Also confirmed each is structurally identical (parsed
  JSON equality) to its corresponding source record in
  `validation_results.jsonl`, matched by `lcats_id` - no reformatting or
  corruption in transit.
- The underlying Opus assessment evidence itself was not regenerated or
  modified - this only moved already-real, already-committed data.
- Flagged to the user (before the approval gate) that the source
  evidence embeds a local absolute filesystem path in each record's
  metadata-rules provenance (e.g. `cache_db_path`) - pre-existing,
  out of this WI's scope to alter (`regenerate_or_modify_validation_evidence`
  is forbidden), promotes verbatim. User approved proceeding as-is.
- A diff-mode `/lrh-self-review` pass before this PR's first push
  independently re-ran the full 146-record validation and byte-identity
  check itself (not trusting this record's own claims): no findings.
  Its top claim (146/146 `validate_sidecar()` pass) was independently
  re-verified by this session directly before accepting the result.

# Validation

- `scripts/version tools` (from `lcats/`): repaired shared-env drift
  (editable install pointed at an unrelated worktree; ruff/black pins
  drifted) before trusting any result - recurred twice during this
  implementation.
- `scripts/format --check --diff`: clean.
- `scripts/lint`: all checks passed.
- `scripts/test` (from `lcats/`): 1932 tests OK.
- `lrh validate`: 208 pre-existing, unrelated errors only (owner-field
  baseline), 0 attributable to this PR.
- Post-promotion: 146/146 promoted sidecars pass `genre_sidecar.
  validate_sidecar()`; 146/146 byte-identical to source evidence
  (independently re-verified by this session, not just the self-review
  subagent's claim).

# Follow-up

- This PR (`https://github.com/xenotaur/LCATS/pull/362`) is intentionally
  left for broader-team review before merge, per explicit user
  instruction - do not merge or close out `WI-GENRE-0077` until that
  review has happened and the user authorizes the merge.
- The one flagged, non-blocking observation (local absolute path
  embedded in pre-existing evidence) was surfaced in the PR body for the
  team's awareness; no action taken since it is out of this WI's scope.
