---
execution_id: 2026_08_13_07_28_42_WI_LLM_0066
prompt_id: PROMPT(WI-LLM-0066:WI_LLM_0066)[2026-08-13T06:29:50+00:00]
work_item: WI-LLM-0066
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/298
commit: 
created_at: 2026-08-13T07:28:42+00:00
agent: codex_app
instruction_source: project/work_items/proposed/WI-LLM-0066.md
session_transcript: codex-app:019fe9db-cb22-7ee3-8629-28dc3d9a87ec
---

# Summary

Implement `WI-LLM-0066`: wire `experiments/04_genre_census/run_census.py`
to an opt-in OpenAI-compatible `--base-url`, run a real local
`gpt-oss:20b` sample against the same 20 stories as the existing Claude
sample, and record a go/no-go recommendation for a full local genre census.

# Result

- Added `--base-url` for `--backend openai`, threading it through
  `_build_backend()` to `OpenAIBackend(base_url=...)` without changing the
  default Anthropic/OpenAI behavior when omitted.
- Scoped checkpoint fingerprints by effective `base_url`, preventing cached
  classifications from one endpoint from being reused against another.
- Added model-and-endpoint-scoped output filenames for endpoint runs, e.g.
  `census_gpt_oss_20b_http_localhost_11434_v1_sample_stories.jsonl`, so the
  local sample does not overwrite the historical Claude sample or another
  endpoint's run.
- Added localhost/loopback detection so Ollama calls report `$0` API cost
  while non-local OpenAI-compatible endpoints remain normal endpoint
  overrides and do not get false zero-cost accounting.
- Committed the real local Ollama `gpt-oss:20b` 20-story sample under
  `experiments/04_genre_census/results/`.
- Documented the measured go/no-go finding in
  `experiments/04_genre_census/README.md`: 20/20 included, `$0.00` API
  cost, 801.5s wall clock, projected ~20.8h for 1,868 stories, 18/20 exact
  `detected_genre` agreement with the Claude sample after normalizing one
  non-canonical local model label.
- Added detected-genre normalization in the census harness so labels such as
  `science_fiction` are counted and written as the canonical
  `science fiction`, with the raw value preserved on the story record.
- Proactive independent self-review found a P2 concern that `--base-url`
  was initially treated as inherently local/free; fixed by distinguishing
  localhost/loopback endpoints from arbitrary remote compatible endpoints.

# Validation

- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/version tools`
  - Python 3.11.9, Ruff 0.15.0, Black 25.11.0 confirmed.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/format --check --diff`
  - 186 files would be left unchanged.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/lint`
  - Ruff all checks passed; Black formatting check passed.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/test`
  - Ran 1712 tests in 9.840s; OK.
- `lrh validate`
  - 0 errors, 139 existing warnings.
- `python experiments/04_genre_census/run_census.py --sample-size 5 --backend openai --base-url http://localhost:11434/v1 --model gpt-oss:20b --dry-run --output /tmp/lcats-wi-llm-0066-dry-run-4`
  - Passed; wrote model-and-endpoint-scoped dry-run files; reported `$0.0000`.
- Real local sample:
  `python experiments/04_genre_census/run_census.py --sample-size 20 --backend openai --base-url http://localhost:11434/v1 --model gpt-oss:20b`
  - Passed; 20/20 included, 0 excluded, `$0.0000` API cost, 801.5s wall
    clock.

# Follow-up

- Before relying on full-corpus local counts as final evidence, review the
  sample's two substantive humor disagreements.
- If the full local census is approved, budget approximately one day of
  local machine time on this hardware.
