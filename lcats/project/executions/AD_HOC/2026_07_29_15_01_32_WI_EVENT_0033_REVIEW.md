---
execution_id: 2026_07_29_15_01_32_WI_EVENT_0033_REVIEW
prompt_id: PROMPT(AD_HOC:WI_EVENT_0033_REVIEW)[2026-07-29T15:01:23-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_07_29_14_46_50_WI_EVENT_0033
pr: https://github.com/xenotaur/LCATS/pull/188
commit: 5a0570c0
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/188
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-07-29T15:01:32-04:00
---

# Summary

Address review feedback on PR #188 (WI-EVENT-0033 implementation).

# Result

Four review comments, all verified directly against the code before
fixing:

1. **P1 (chatgpt-codex-connector)**: `openai_backend.py`'s tool
   construction omitted the function-level `strict` flag OpenAI's API
   needs for schema-constrained decoding, forwarding only
   `name`/`description`/`parameters`. Confirmed by reading the code
   directly — silently defeats strict-mode hardening for **every**
   extractor whose `default_model` resolves to OpenAI, including
   WI-EVENT-0032's seven ERW schemas, not just this PR's three (all
   default to `gpt-4o`). Fixed by forwarding `tool.get("strict", False)`
   into the function dict; added a direct test asserting it's forwarded.
2. **P2 (chatgpt-codex-connector)**: my first draft pushed the
   `"segments"` wrapper-key shape change onto both real call sites
   (`story_processors.py`, `run_pilot.py`'s `_segment_story`), which
   would also have broken `notebooks/12_extract_scenes.ipynb` (out of
   scope to edit per `AGENTS.md`'s "do not edit notebooks" rule).
   Confirmed the notebook's exact break by reading its cells directly.
   Better fix: preserve the bare-list contract in library code instead
   of pushing it onto every caller. Added
   `scene_analysis._segment_result_aligner`/`_segment_result_validator`
   wrapper functions that pass the wrapped `{"segments": [...]}` shape
   through to `text_segmenter`'s real aligner/auditor (which still need
   it), then unwrap to a bare list before `extracted_output` is ever
   returned. Reverted both call-site changes (no longer needed) and
   confirmed directly (ran the exact notebook usage pattern manually)
   that it now works unmodified.
3. **copilot-pull-request-reviewer (×2)**:
   `SCENE_SEMANTICS_USER_PROMPT_TEMPLATE` and
   `DOC_CLASSIFY_SYSTEM_PROMPT`/`DOC_CLASSIFY_USER_PROMPT_TEMPLATE` still
   instructed the model to nest its JSON under top-level `"judgment"`/
   `"classification"` keys, contradicting the new no-wrapper schemas — a
   real prompt/schema mismatch risking degraded output quality (or
   invalid arguments under non-strict/OpenAI paths) even though strict
   grammar-constrained decoding structurally prevents the literal
   wrapper from appearing in the tool arguments themselves. Updated both
   prompts to describe the flat shape directly, matching the schemas
   exactly.

# Validation

- `pytest tests/analysis_tests/scene_analysis_test.py
  tests/analysis_tests/story_analysis_test.py
  tests/analysis_tests/story_processors_test.py
  tests/llm_tests/openai_backend_test.py` (from `lcats/`) — all pass.
- `scripts/test` (from `lcats/`) — full suite, 1505 passed (up from
  1493).
- `pytest experiments/03_cross_segment_relation_pilot/run_pilot_test.py`
  — 3 passed.
- `scripts/lint` (from `lcats/`) plus direct `black`/`ruff` on the two
  `experiments/` files — all clean.
- `lrh validate` (from `lcats/`) — 0 errors.
- Manually ran the exact sequence
  `notebooks/12_extract_scenes.ipynb` uses
  (`extract()['extracted_output']` passed to `display_segments`/
  `annotate_segments_with_semantics`) directly in a Python shell to
  confirm the notebook genuinely still works unmodified.
- All 4 review threads resolved via `gh api graphql resolveReviewThread`
  after the fixes landed.

# Follow-up

None beyond what the primary execution record already lists (the live
smoke-test re-run still needs real credentials).
