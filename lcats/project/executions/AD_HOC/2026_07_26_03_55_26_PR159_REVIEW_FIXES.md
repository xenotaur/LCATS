---
execution_id: 2026_07_26_03_55_26_PR159_REVIEW_FIXES
prompt_id: PROMPT(AD_HOC:PR159_REVIEW_FIXES)[2026-07-26T03:55:19-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/159
commit: 
created_at: 2026-07-26T03:55:26-04:00
---

# Summary

Apply direct fixes to `00_proposal.md` in response to PR #159's landed
automated review (Copilot + Codex), following the land-to-closeout
skill's "apply fixes directly" path rather than routing through
`/lrh-review-response` for the fix itself.

# Result

Five findings from the landed review were verified against the repo and
corrected in
`project/design/proposals/proposed/lcats-packaging-modernization/00_proposal.md`:

1. Copilot — gap 6's claim that tool versions are "pinned only in CI" was
   inaccurate (`black` is already pinned in the `test`/`dev` extras); reworded
   to state precisely what is and isn't pinned where.
2. Copilot — cross-references to internal project-memory keys
   (`feedback_precommit_hook_rewrite_version_skew`,
   `project_ci_pins_formatting_source_of_truth`) are not discoverable by a
   repo reader; replaced with an inline statement of the underlying
   first-hand observation instead of an undiscoverable ID.
3. Codex (P2) — the proposed `setuptools>=68` floor is insufficient for the
   PEP 639 `license`/`license-files` change also proposed in Phase 1;
   PEP 639 support was added in setuptools 77.0.0 per the setuptools
   changelog (verified via web search against
   https://setuptools.pypa.io/en/latest/history.html). Raised the floor to
   `setuptools>=77` for that phase.
4. Codex (P1) — Phase 3 enabled `dynamic = ["version"]` without adding
   `setuptools-scm` to `build-system.requires`; a PEP 517 isolated build
   only installs declared build requirements, so the backend could not
   invoke the SCM plugin. Added the missing requirement to the Phase 3
   implementation-plan text.
5. Codex (P2) — the Phase 2 blast-radius list omitted
   `experiments/02_llm_backend_comparison/run_comparison.py` and
   `experiments/03_cross_segment_relation_pilot/run_pilot.py`, both of
   which `sys.path.insert` the pre-move `<repo>/lcats` package path to
   support direct execution without an editable install (confirmed by
   reading both files). Added both to the Phase 2 audit list with the
   specific `sys.path` fix needed.

# Validation

- `lrh validate` from `lcats/`: 0 errors, 41 warnings (pre-existing,
  unrelated to this file).
- Verified extras pinning claim by re-reading
  `lcats/pyproject.toml` `test`/`dev` extras directly.
- Verified `sys.path` bootstrap claim by grepping both `experiments/*/run_*.py`
  files for `sys.path`/`lcats` references.
- Verified the setuptools 77.0.0 PEP 639 claim via WebSearch against the
  setuptools changelog.

# Follow-up

None — proceeding to `/lrh-review-response` (expected to find nothing left
to do) and `/lrh-confirm-fixes` to resolve the review threads.
