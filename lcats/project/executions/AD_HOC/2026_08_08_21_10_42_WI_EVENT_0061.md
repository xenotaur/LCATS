---
execution_id: 2026_08_08_21_10_42_WI_EVENT_0061
prompt_id: PROMPT(AD_HOC:WI_EVENT_0061)[2026-08-08T21:09:16+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/268
commit: 978aca184d37247908f30659e95886c0a8cb6b2f
created_at: 2026-08-08T21:10:42+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-EVENT-0061.md
session_transcript: claude-app:693d6013-727b-422d-a378-5dc4242d3076
---

# Summary

Scoped `WI-EVENT-0061` via `/lrh-work-item`: guard `build_*()`
array-field iteration against non-list values across the six
`event_role_world/` extractors, addressing `backlog.md`'s "Malformed-item
guards check each item's type but never the container's" P1 entry.

# Result

- Prior-art check: no existing container-type guard anywhere in
  `event_role_world/` (confirmed via grep); no open WI/PR/branch already
  addressing this (confirmed 2026-08-08 across all work item buckets,
  open PRs, and every active local worktree).
- Re-verified all 12 array-item iteration sites directly against current
  code -- several line numbers had shifted since the backlog entry was
  written, and `discourse_extractor.py` actually has 3 sites
  (`speech_acts`, `explanations`, `sf_tags`), not the 1 the backlog named.
  The corrected count (12) matches `WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY`'s
  own exit criteria for the sibling item-level guard work, cross-checking
  that no site was missed.
- Also corrected `run_pilot.py`'s cited line numbers (1211/1347 in the
  current tree, not the backlog's stale 1100/1328).
- Linked to `WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY` (same extractor
  family and reliability theme as `WI-EVENT-0032`/`0033`, though this bug
  wasn't part of the original audit that spawned that workstream) --
  offered to the user for a workstream-file update; not yet applied
  pending their answer.
- Opened [PR #268](https://github.com/xenotaur/LCATS/pull/268).

# Validation

- `lrh validate` -- 0 errors (127 pre-existing warnings, all
  `owner: unassigned`-related, unrelated to this WI).

# Follow-up

- This record documents WI *creation* only, not implementation --
  `status` stays `in_progress` until `/lrh-closeout` lands PR #268.
- Once merged: consider executing via `/lrh-execute WI-EVENT-0061`.
- Workstream update (`WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY`'s
  `work_items:` list) offered but not yet confirmed/applied -- see the
  session for the user's answer.
