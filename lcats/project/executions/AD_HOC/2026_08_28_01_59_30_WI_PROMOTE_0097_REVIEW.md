---
execution_id: 2026_08_28_01_59_30_WI_PROMOTE_0097_REVIEW
prompt_id: PROMPT(AD_HOC:WI_PROMOTE_0097_REVIEW)[2026-08-28T01:59:24+00:00]
work_item: AD_HOC
status: landed
rerun_of: 2026_08_28_01_37_12_LCATS_PROMOTE_MODE_REDESIGN_ADOPT
pr: https://github.com/xenotaur/LCATS/pull/401
commit: a639fe837274380ac106d5e9c7e1dfb5865630dc
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/401
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-28T01:59:30+00:00
---

# Summary

Review-response round for PR #401 (adopt `PROP-LCATS-PROMOTE-MODE-
REDESIGN` + activate `WS-PROMOTE-MODE-REDESIGN` + mint
`WI-PROMOTE-0097`). The PR's automatic first-push review surfaced 2 real
P2 findings from `chatgpt-codex-connector`, both design gaps in
`WI-PROMOTE-0097`'s own scope (no code exists yet).

# Result

- **Manifest identity envelope gap (fixed)**: `promote_sidecar_tranche()`
  derives destination routing from a manifest record's own top-level
  `lcats_id` field - independently verified against `annotate.py`'s
  `_annotate_scenes()` that `scenes.json` payloads carry no
  story-identity field at all (only `segments`/`segment_count`/`model`/
  `input_tokens`/`output_tokens`), unlike `genre-sidecar-v1` payloads.
  The WI's own acceptance criteria require supporting all 4 registered
  kinds including `scenes.json`, so this was a real, unaddressed gap.
  Fixed by changing Required Change 2 to specify an envelope manifest
  format (`{"lcats_id": ..., "payload": ...}`) that separates
  destination routing from payload content, working uniformly across
  all 4 kinds without needing live-directory-scan sourcing (out of
  scope for this item). Flagged the resulting migration question for
  existing genre-sidecar-v1 manifests as a Risk Note rather than
  silently deciding it.
- **`--allow-unvalidated` scope ambiguity (fixed)**: the adopted
  proposal explicitly left "does it bypass a registered-but-failing
  validator, or only cover unregistered kinds" as an Open Question;
  the WI carried that forward unresolved in its acceptance criteria.
  Pinned to the narrower, safer reading - covers only the
  no-registered-validator case; a registered validator's own rejection
  is never bypassable in this item. Added a corresponding
  `forbidden_actions` entry and updated the adopted proposal's own Open
  Questions section to record the resolution, closing the loop where
  the ambiguity was originally raised.
- No scope creep: both fixes amend the WI's own stated scope and the
  proposal's Open Questions; neither pulls Stage 2/3 work into this
  item.

# Validation

- `lrh validate`: targeted check on both modified files reports 0
  errors (baseline owner-unassigned warning on the WI unchanged).
- Manual re-read of both fixed sections against the actual review
  comments to confirm each fix addresses exactly what was raised.

# Follow-up

- None outstanding from this round. Proceeding to confirm-fixes next to
  verify the fixes against the current diff and resolve the review
  threads.
