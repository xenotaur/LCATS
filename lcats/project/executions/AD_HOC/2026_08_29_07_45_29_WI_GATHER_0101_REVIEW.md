---
execution_id: 2026_08_29_07_45_29_WI_GATHER_0101_REVIEW
prompt_id: PROMPT(AD_HOC:WI_GATHER_0101_REVIEW)[2026-08-29T07:44:03+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_29_06_29_52_WI_GATHER_0101
pr: https://github.com/xenotaur/LCATS/pull/412
commit: d56ce8c1e8f72fae8759fe3a004518a7a9ef9845
created_at: 2026-08-29T07:45:29+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/412
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-review-response https://github.com/xenotaur/LCATS/pull/412` —
addressed 5 review comments (3 Codex, 2 Copilot), all substantive
corrections to the investigation's own stated premise.

# Result

1. **Genuine premise error: `mass_quantities`' error handling
   overstated.** `gather_stories()`'s `failed_stories` return is not
   general per-story exception isolation — `parser.gather_story()`
   (`parser.py:1365-1405`) only wraps `api.load_etext()` in
   `try`/`except`; metadata access, parsing, normalization, directory
   creation, and JSON writes are unprotected and can still propagate.
   Corrected the WI's Problem/Context to state this precisely, flagging
   it as something the audit itself must verify directly rather than
   assume.
2. **Genuine premise error: Lovecraft mischaracterized as
   "near-identical"** to `gatherlib.gather()`. It shares the loop shape
   but each story is its own separate Gutenberg URL via a per-entry
   `Extractor` object (not one shared `gutenberg_url`), and it extracts
   via `extract_text_between_ids()` rather than the heading-based
   `paragraph_finder` callback shape. Corrected — any reconciliation
   here would need `gatherlib.gather()` to accept a per-entry URL and a
   pluggable extraction strategy, not just a rename.
3. **Citation misattribution.** The 3-gatherer exclusion is from
   `WI-RUNLOG-0082`'s own Non-Goals (lines 113-118), not
   `PROP-LCATS-RUN-LOG`'s — the proposal's Decision 4 table classifies
   `lcats gather` as one aggregate upgrade site and doesn't itself name
   the three. Corrected.
4. **Path citations unfollowable.** Both the WI and its execution record
   used short-form paths (`sherlock/gatherer.py`) instead of the real
   repo-relative path (`lcats/src/lcats/gatherers/sherlock/gatherer.py`).
   Fixed in both files.

All 5 genuine and fixed; none skipped. No test/lint/format validation
needed — this PR only touches Markdown planning documents, no Python
files.

# Validation

- `lrh validate` — exit 0, only pre-existing unrelated warnings.

# Follow-up

- Reminder: `session_transcript` should be confirmed/updated at closeout
  time if it differs from the live `CLAUDE_CODE_HOST_SESSION_ID`
  convention.
- Next: `/lrh-confirm-fixes` to verify the fixes against the current diff
  and resolve the review threads.
