---
execution_id: 2026_08_20_04_39_39_WI_SEGMENT_0070
prompt_id: PROMPT(AD_HOC:WI_SEGMENT_0070)[2026-08-20T04:31:11+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/321
commit: dceed659a4e74384a383377142576aea4c93e5cd
created_at: 2026-08-20T04:39:39+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-SEGMENT-0070.md
session_transcript: claude-app:693d6013-727b-422d-a378-5dc4242d3076
---

# Summary

Scoped `WI-SEGMENT-0070` via `/lrh-work-item`: fix segmentation alignment
anchor-matching gaps (paragraph-index-marker leakage and Unicode
quote/dash typography mismatches), per the "Fix now" recommendation in
`WI-SEGMENT-0069`'s investigation design doc.

# Result

- Two concrete, narrowly-fixable sub-patterns identified in
  `WI-SEGMENT-0069`'s investigation (`lcats/project/design/segmentation-alignment-failure-categories.md`):
  paragraph-index-marker leakage (e.g. `[P0047]`) into the model's anchor
  text, and Unicode curly-quote/dash typography mismatches -- together
  5/21 (~24%) of that investigation's observed `alignment_error`
  failures.
- Scoped this as a `deliverable`-type WI, narrow the same way
  `WI-SEGMENT-0068` was: a targeted transform in
  `_locate_anchor_span` before matching, explicitly forbidding a widened
  search range or full-document fallback.
- `depends_on: [WI-SEGMENT-0069]` -- confirmed `status: resolved` before
  creating this WI.
- Prior-art check: no existing WI addresses either sub-pattern; this
  item is the direct implementation of `WI-SEGMENT-0069`'s own
  design-doc recommendation, not an independent new decision.
- Opened [PR #321](https://github.com/xenotaur/LCATS/pull/321).

# Validation

- `lrh validate` -- 0 errors (156 warnings, 2 new and matching the same
  `owner: unassigned` pattern already present on every other work item,
  unrelated to this WI).

# Follow-up

- This record documents WI *creation* only, not the fix itself --
  `status` stays `in_progress` until `/lrh-closeout` lands PR #321.
- Once merged: execute via `/lrh-execute WI-SEGMENT-0070`.
