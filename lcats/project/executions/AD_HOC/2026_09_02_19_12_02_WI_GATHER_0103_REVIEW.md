---
execution_id: 2026_09_02_19_12_02_WI_GATHER_0103_REVIEW
prompt_id: PROMPT(AD_HOC:WI_GATHER_0103_REVIEW)[2026-09-02T19:11:51+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_09_02_19_05_04_WI_GATHER_0103
pr: https://github.com/xenotaur/LCATS/pull/421
commit: 
created_at: 2026-09-02T19:12:02+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/421
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-review-response https://github.com/xenotaur/LCATS/pull/421` —
addressed 2 review comments (1 Copilot docstring nit, 1 Codex P1
substantive testing-philosophy violation).

# Result

1. **Genuine, P1: mocking `gatherlib.gather()` itself, not a true
   boundary.** The initial implementation's `TestGather` tests patched
   `gatherlib.gather()` directly, verifying only that it was called with
   the right arguments — per `AGENTS.md:42-44` ("avoid heavy mocking...
   mock only at true boundaries") this was exactly the anti-pattern the
   project's own testing philosophy warns against, and it lost real
   coverage: the retired `TestCreateDownloadCallback` tests used to
   exercise `find_paragraphs_adventures` against real HTML end-to-end,
   and nothing replaced that. Rewrote `TestGather` to mock
   `lcats.gatherers.gatherlib.downloaders.DataGatherer` instead — the
   actual network/external boundary, matching `gatherlib_test.py`'s own
   precedent for testing `gatherlib.gather()` — and added a new test
   that captures the real callback `gather()` hands to `download()` and
   invokes it with real HTML, verifying `find_paragraphs_adventures`
   output flows through correctly (description, text, metadata).
   - **Self-caught while fixing, before pushing:** the new end-to-end
     test doesn't override `log_dir`, and `sherlock/gatherer.py`'s
     `gather()` doesn't expose one — so it wrote a real
     `logs/gather/sherlock_holmes_gather_run_log.jsonl` into the repo
     working tree the first time it ran. Fixed by adding
     `setUp`/`tearDown` to `TestGather` that `chdir`s into a
     `tempfile.TemporaryDirectory()` for the duration of each test,
     matching `gatherlib_test.py`'s own tempdir-isolation pattern (which
     avoids this by passing an explicit `log_dir` directly — not
     available here since `sherlock.gather()` takes no arguments).
2. **Genuine, low: `gather()`'s docstring was stale.** Still said "Run
   DataGatherers for the Sherlock Holmes corpus," describing direct
   `DataGatherer` usage that no longer exists. Updated to "Gather the
   Sherlock Holmes corpus via gatherlib.gather()."

Both genuine and fixed; none skipped.

# Validation

- Full test suite (`PYTHONPATH`-corrected) — 2245/2245 pass (2 more than
  before this fix, matching the added end-to-end test).
- `scripts/format --check --diff` / `scripts/lint` — clean.
- `lrh validate` — exit 0.
- Confirmed no leftover `logs/` artifact in the working tree after the
  fix (there was one before it, from the first uncorrected test run).

# Follow-up

- Next: `/lrh-confirm-fixes` to verify these fixes against the current
  diff and resolve the review threads.
