---
execution_id: 2026_08_21_22_16_23_DOC_ORGANIZE_PHASE_2C_2026_08_21
prompt_id: PROMPT(AD_HOC:DOC_ORGANIZE_PHASE_2C_2026_08_21)[2026-08-21T18:57:46+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/339
commit: 
agent: claude_app
instruction_source: project/audits/docs/docs-audit-2026-08-21.md
session_transcript: pending
created_at: 2026-08-21T22:16:23+00:00
---

# Summary

`/lrh-doc-organize` Phase 2c: implements `project/audits/docs/docs-audit-2026-08-21.md`'s
"Proposed first PR scope" — navigation/accuracy fixes only, no new
authored content beyond flag references verified against real behavior.

# Result

- Fixed 3 real broken links (worldcon workstream retarget, WI-STATS-0049
  relative-path depth, corpus README's 2 links one `../` short).
- Fixed all 9 stale `lcats/lcats/` → `lcats/src/lcats/` path references
  across 4 files.
- Documented the `annotate` CLI subcommand in `cli-status.md` and
  `cli-commands.md`, verified against real `lcats annotate --help`
  output.
- Added `promote`/`clean`/`annotate`/`linguistics` to the repo-root
  README's CLI table (scope expanded by one item — `linguistics` — with
  user confirmation, since it was the same table edit and also missing);
  fixed stale Python version and OpenAI-only LLM-provider wording.
- Linked `experiments/04_genre_census/` and
  `experiments/05_metadata_genre_prefilter/` from `docs/index.md`.
- One originally-scoped item (pointer-ifying the corpus README's §9)
  dropped — verified concurrent work already resolved it before this PR
  started.

Re-verified the full scope against `main`'s state twice during this run
(main advanced by 23 commits between branch creation and implementation)
before implementing, to avoid acting on a stale audit snapshot.

Ran a diff-mode `/lrh-self-review` pass (cold-context subagent +
independent re-verification of the top claim — the `annotate` CLI
documentation's accuracy, re-checked directly against
`lcats annotate --help` output) before the first push. Clean, no
findings.

# Validation

- `lrh validate` → 0 new errors (166 pre-existing warnings unrelated to
  touched files)
- Full repo-wide link check → 0 real broken links (6 known
  methodology-description false positives remain, unchanged)
- `annotate` docs verified verbatim against real `--help` output
- `requires-python = ">=3.10"` verified in `lcats/pyproject.toml`
- Docs-only diff; `scripts/lint`/`scripts/format` blocked locally by a
  pre-existing tool-version mismatch unrelated to this change (no Python
  files touched) — CI will verify

# Follow-up

Remaining audit phases (2d: repo-root README Phase 2b leftovers already
folded into this PR; Phase 5: experiments navigation, done here; Phase 6:
new Explanation content for local-model-evaluation and corpus-analysis
architecture) — Phase 6 is the only substantial remaining phase, deferred
as it requires authored content, not navigation/accuracy-only fixes.
