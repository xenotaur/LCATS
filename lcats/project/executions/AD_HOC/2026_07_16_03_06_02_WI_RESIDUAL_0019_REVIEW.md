---
execution_id: 2026_07_16_03_06_02_WI_RESIDUAL_0019_REVIEW
prompt_id: PROMPT(AD_HOC:WI_RESIDUAL_0019_REVIEW)[2026-07-16T02:45:29-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_07_15_22_06_07_WI_RESIDUAL_0019
pr: https://github.com/xenotaur/LCATS/pull/124
commit: 03ff085
created_at: 2026-07-16T03:06:02-04:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/124
session_transcript: claude-app:ff20b6b0-c572-4847-9c89-034d6aa827cd
---

# Summary

Address three review issues on PR #124 (WI-RESIDUAL-0019) via
`lrh request review_response`. Four comments from chatgpt-codex-connector and
copilot-pull-request-reviewer, reducing to three distinct issues (two
comments about wiring the allowlist are duplicates).

# Result

All three fixed in commit 03ff085, each confirmed against code before fixing:

- Wire the packaged allowlist into survey defaults (r3592134963,
  r3592135546) — both `cli.py` and `specials_cli.py` defaulted
  `--allowlist-config` to `""`, so the WI's own acceptance command
  (`lcats survey --mode specials data/`) never applied the seeded
  allowlist unless the caller knew the package-internal path. Added
  `specials.default_allowlist_config_path()`, wired as the CLI default in
  both files (falls back to `""` if the packaged file is somehow missing,
  rather than crashing).
- Two invisible test characters (r3592135505, r3592135529) — replaced a
  literal NO-BREAK SPACE and literal U+FEFF BOM with explicit
  `\xa0`/`﻿` escapes in `specials_test.py`.

Investigation before fixing surfaced a subtlety: `unicode.py`'s
pre-existing, separate `DEFAULT_EXCLUDED_CHARS` list already
blanket-excludes Â/Ã/â and many accented letters via a different code path
than the new JSON allowlist — an initial sanity check on "Ângstrom" looked
quiet even before the fix, for the wrong reason. Confirmed the fix is
still necessary and sufficient by testing exactly the reviewer's cited
characters: `lcats survey` now reports zero findings for `5¢ ¶ ·` by
default (was 3 findings with `--allowlist-config ""`). Added a permanent
regression test for this.

No comments skipped.

# Validation

- `black --check` — clean
- `scripts/lint` — ruff and black passed
- `scripts/test` — 1297 tests OK (2 new)
- `lrh validate` — 0 errors, 24 pre-existing owner-role warnings

# Follow-up

- On merge: set this record and the primary WI-RESIDUAL-0019 record to
  landed, and resolve WI-RESIDUAL-0019.
- Architectural note (not actioned, out of scope): two parallel
  legitimate-character exclusion mechanisms now exist (`unicode.py`'s
  hardcoded lists and the new JSON allowlist). Worth consolidating
  eventually, not blocking this workstream.
