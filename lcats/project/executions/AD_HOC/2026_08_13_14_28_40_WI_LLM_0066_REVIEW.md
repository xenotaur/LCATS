---
execution_id: 2026_08_13_14_28_40_WI_LLM_0066_REVIEW
prompt_id: PROMPT(AD_HOC:WI_LLM_0066_REVIEW)[2026-08-13T07:32:44+00:00]
work_item: AD_HOC
status: landed
agent: codex_app
instruction_source: https://github.com/xenotaur/LCATS/pull/298
session_transcript: codex-app:019fe9db-cb22-7ee3-8629-28dc3d9a87ec
rerun_of: 2026_08_13_07_28_42_WI_LLM_0066
pr: https://github.com/xenotaur/LCATS/pull/298
commit: 661b878c5b8ebfca0243d6f6ea27383f8ed0077e
created_at: 2026-08-13T14:28:40+00:00
---

# Summary

Address automatic review feedback on PR #298 for WI-LLM-0066 without
triggering additional GitHub review agents.

# Result

- Made local endpoint result filenames model-and-endpoint scoped, e.g.
  `census_gpt_oss_20b_http_localhost_11434_v1_sample_*`, so runs for the same
  model against different OpenAI-compatible endpoints cannot overwrite each
  other.
- Changed `local_call_count` to count fresh metered local calls only, excluding
  checkpoint/cache hits and zero-token dry-run records.
- Wrapped optional historical-reference comparison loading so an unreadable or
  malformed `census_{mode}_stories.jsonl` file records
  `reference_comparison_error` instead of aborting summary writing after a
  completed run.
- Preserved compatibility with existing default-endpoint checkpoints by adding
  `base_url` to the checkpoint fingerprint only when an override endpoint is
  explicitly supplied.
- Gated optional historical-reference comparison to sample-mode endpoint runs,
  avoiding unnecessary full-corpus comparison payloads.
- Renamed the committed local `gpt-oss:20b` sample result artifacts to the new
  endpoint-aware prefix and updated experiment documentation plus the original
  WI execution record accordingly.

# Validation

- `PATH=<LCATS conda env bin>:$PATH scripts/format -- ../experiments/04_genre_census/run_census.py tests/experiment_tests/run_census_test.py`
  - Passed; 2 files left unchanged.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/lint ../experiments/04_genre_census/run_census.py tests/experiment_tests/run_census_test.py`
  - Passed.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH python -m unittest tests/experiment_tests/run_census_test.py`
  - Passed; 12 tests OK.
- `PYTHONPATH=lcats/src PATH=<LCATS conda env bin>:$PATH python experiments/04_genre_census/run_census.py --sample-size 5 --backend openai --base-url http://localhost:11434/v1 --model gpt-oss:20b --dry-run --output /tmp/lcats-wi-llm-0066-review-dry-run-2`
  - Passed; wrote endpoint-aware dry-run files and reported 0 local calls for
    dry-run records.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/version tools`
  - Python 3.11.9, Ruff 0.15.0, Black 25.11.0 confirmed.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/format --check --diff`
  - Passed with escalation for the known Black multiprocessing socket sandbox
    issue; 186 files would be left unchanged.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/lint`
  - Passed.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH scripts/test`
  - Passed; 1712 tests OK.
- `PYTHONPATH=src PATH=<LCATS conda env bin>:$PATH lrh validate`
  - Passed; 0 errors, 139 existing warnings.

# Follow-up

- Continue PR #298 landing once CI and automatic review state are green.
