---
execution_id: 2026_08_31_06_55_03_WI_PROMOTE_0102_REVIEW
prompt_id: PROMPT(AD_HOC:WI_PROMOTE_0102_REVIEW)[2026-08-31T06:53:02+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_31_01_51_25_WI_PROMOTE_0102
pr: https://github.com/xenotaur/LCATS/pull/417
commit: 78fd8311
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/417
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-31T06:55:03+00:00
---

# Summary

Review-response round for PR #417 (`WI-PROMOTE-0102` creation). Four
review comments landed (2 Copilot, 2 Codex).

# Result

- **"usage sites" wording (fixed, Copilot)**: reworded "two ... imports"
  to "two ... usage sites" in the Summary and the first acceptance
  criterion -- one import statement, two call sites.
- **forbidden_actions naming inconsistency (fixed, Copilot)**: renamed
  `modify_replace_wholesale_mechanism` to
  `change_promote_wholesale_replacement_mechanism`, matching the name
  `WI-PROMOTE-0101` already uses for the same constraint.
- **False PR #405 history claim (fixed, real P2, Codex)**: independently
  re-verified before fixing. My original `git show <merge-commit>` check
  used the combined-diff form, which hides changes reflected identically
  on the merge-branch side; diffing against the actual mainline parent
  (`git diff <merge-commit>^1 <merge-commit>`) shows PR #405 *did*
  restructure `_promote_sidecar_records`'s legacy-flat-overwrite guard --
  nesting it under a new `sidecar_filename == GENRE_SIDECAR_FILENAME`
  conditional while generalizing the promotion engine to multiple
  sidecar kinds -- though the guard's own check logic and its
  `genre_sidecar.is_legacy_flat_sidecar()` call were preserved unchanged.
  `_validate_sidecars` (replace's structural check) was confirmed
  genuinely untouched. Corrected the acceptance criterion and
  Problem/Context to state this accurately per-usage rather than
  claiming both were untouched.
- **Workstream closure gating (skipped, Codex P2)**: valid governance
  point, but describes an intentional design decision (not registering
  this WI in `WS-PROMOTE-MODE-REDESIGN`'s `work_items:` list) already
  proposed and confirmed by the user at WI-creation time -- not a defect
  in this WI file. Not applied as an automatic edit.

# Validation

- `scripts/version tools`: ruff/black drifted mid-session again (black
  26.3.1 vs. pinned 25.11.0); re-pinned via `pip install -q
  "ruff==0.15.0" "black==25.11.0"`, re-verified.
- `scripts/format --check --diff`: clean, no reformatting needed.
- `lrh validate`: 0 errors, only the standard `owner: unassigned`
  warnings.

# Follow-up

- None outstanding from this round. Proceeding to confirm-fixes next.
