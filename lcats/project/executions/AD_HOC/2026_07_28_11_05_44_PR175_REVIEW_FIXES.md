---
execution_id: 2026_07_28_11_05_44_PR175_REVIEW_FIXES
prompt_id: PROMPT(AD_HOC:PR175_REVIEW_FIXES)[2026-07-28T11:05:36-04:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/175
commit: 
created_at: 2026-07-28T11:05:44-04:00
---

# Summary

Apply direct fixes to docs and scripts in response to PR #175's landed
automated review (Copilot + Codex), following the land-to-closeout
skill's "apply fixes directly" path rather than routing through
`/lrh-review-response` for the fix itself.

# Result

1 thread, Codex (P2), citing 4 broken references — all confirmed real by
reading each cited file/line directly:

1. `lcats/docs/index.md:36` — relative link `../lcats/analysis/corpus/README.md`
   pointed at the deleted path; fixed to `../src/lcats/analysis/corpus/README.md`.
2. `lcats/docs/tutorials/quickstart.md:137` — same broken link pattern,
   fixed to `../../src/lcats/analysis/corpus/README.md`.
3. `lcats/docs/reference/prepare-corpora-release.md:252` — same, fixed to
   `../../src/lcats/analysis/corpus/README.md`.
4. `lcats/scripts/README.md:111` — example command
   `scripts/lint lcats/gatherers/downloaders.py` referenced a nonexistent
   path; fixed to `scripts/lint src/lcats/gatherers/downloaders.py` and
   verified the corrected command actually runs clean.

While fixing #4, also updated two more stale `lcats/` references in the
same doc (`scripts/README.md`'s "Applies formatting to"/"Targets
(default)" lists, both said `lcats/` instead of `src/`) and found a real
gap the original WI missed: `lcats/scripts/clean` hardcoded
`rm -rf build dist lcats.egg-info`, but the egg-info directory now lives
at `lcats/src/lcats.egg-info` (confirmed via `pip install -e .` in the
`LCATS` conda env) — fixed both the script and its README description.

All 4 link/path fixes verified by direct resolution (`ls` against the
relative paths) rather than just editing and hoping.

# Validation

- `lrh validate` from `lcats/`: 0 errors, 47 warnings (pre-existing,
  unrelated).
- `scripts/format --check --diff`, `scripts/lint`, `scripts/test`
  (`Ran 1449 tests — OK`): all pass, run in the `LCATS` conda env.
- Verified each doc link resolves via `ls <relative-path>` from its
  source file's directory.
- Ran the corrected `scripts/lint src/lcats/gatherers/downloaders.py`
  example command directly — passes clean.

# Follow-up

None — proceeding to `/lrh-confirm-fixes` to resolve the review thread.
