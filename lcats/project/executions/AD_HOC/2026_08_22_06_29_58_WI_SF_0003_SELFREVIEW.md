---
execution_id: 2026_08_22_06_29_58_WI_SF_0003_SELFREVIEW
prompt_id: PROMPT(AD_HOC:WI_SF_0003_SELFREVIEW)[2026-08-22T06:29:53+00:00]
work_item: AD_HOC
status: in_progress
rerun_of:
pr:
commit:
agent: codex_app
instruction_source: lrh-self-review diff-mode for WI-SF-0003
session_transcript: pending
created_at: 2026-08-22T06:29:58+00:00
---

# Summary

Ran `/lrh-self-review` diff-mode before opening the WI-SF-0003 PR.
The review target was the local implementation diff for the shared
science-fiction neutral evidence layer.

# Result

Mode: diff.

Findings: 3.

- P1, procedural: the review agent observed that the implementation files
  were untracked when it ran `git diff main`, so its local diff appeared
  empty. This was an artifact of the report-only self-review index reset;
  the files are intentionally staged later by `/lrh-implement` Step 8.
- P1, verified and fixed: invalid candidate offsets could be accepted as
  anchors because Python slicing allowed negative indexes. The fix rejects
  missing, inverted, out-of-bounds, or mismatched explicit offsets before
  creating an `EvidenceAnchor`.
- P2, verified and fixed: declared paragraph or chunk constraints could be
  ignored by falling back to whole-story search. The fix treats supplied
  chunk and paragraph anchors as hard constraints and quarantines candidates
  that cannot be located within them.

Top-finding re-verification: the procedural untracked-file finding was
real for the self-review agent's checkout state, but not a code defect.
The most severe code finding was independently reproduced with a negative
offset candidate before applying the fix.

Fixes applied: yes, in the main session after direct verification.

# Validation

- `PYTHONPATH=src python -m unittest discover -s tests/analysis_tests/science_fiction -p '*_test.py'` — 22 tests OK.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/format --check --diff` — 200 files would be left unchanged.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/lint` — Ruff passed; Black formatting check passed.
- `PATH=/Users/centaur/anaconda3/bin:$PATH scripts/test` — 1855 tests OK after refreshing this checkout's editable install with `scripts/develop`.
- `lrh validate` — 0 errors, 178 inherited warnings.

# Follow-up

None for this self-review pass.
