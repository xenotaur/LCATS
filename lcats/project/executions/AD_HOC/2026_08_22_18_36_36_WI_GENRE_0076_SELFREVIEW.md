---
execution_id: 2026_08_22_18_36_36_WI_GENRE_0076_SELFREVIEW
prompt_id: PROMPT(AD_HOC:WI_GENRE_0076_SELFREVIEW)[2026-08-22T18:36:27+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: 
commit: 14594dedda22424b91e181cbf6eebbbeab6d21a6
agent: claude_app
instruction_source: lcats/project/work_items/proposed/WI-GENRE-0076.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-22T18:36:36+00:00
---

# Summary

`/lrh-self-review` diff-mode pass for `WI-GENRE-0076`'s implementation,
before the PR's first push. `rerun_of` and `pr` are left blank by
construction - this runs before `/lrh-implement` Step 9 creates the
primary record or Step 8 opens the PR. `commit` records the branch's
`origin/main` fork point, since the diff itself is still uncommitted
working-tree content at this point.

# Result

- Dispatched a cold-context `general-purpose` subagent against
  `git diff origin/main` (scoped to `annotate.py`/`annotate_test.py`)
  plus the WI's own 7 acceptance criteria and `genre_sidecar.py` for
  orientation.
- Subagent found 3 genuine issues, all confirmed by actually running
  reproduction code, not just reading the diff:
  1. **Checkpoint-hit duplicate append (real bug)**: a resumed
     `lcats annotate` run under an unchanged config appended a fresh,
     content-identical `model_detect` assessment on every re-run
     (reproduced: 1 -> 2 assessments across two calls with zero new
     backend calls), contradicting the module's own checkpoint
     idempotency guarantee. **Fixed**: `_annotate_genre` now returns a
     third `from_cache` flag; `annotate_story` skips the append when a
     checkpoint hit lands on an already-materialized sidecar file (still
     materializes it if the file was deleted, preserving the existing
     `test_resumed_sidecar_rewritten_from_checkpoint_data` test's own
     contract).
  2. **`current_adjudication` silently dropped on merge (real bug)**:
     `merge_genre_sidecar` rebuilt the top-level record from scratch
     without carrying forward an existing `current_adjudication` pointer
     - a real data-loss trap for the future human-adjudication workflow
     this same diff's `build_human_genre_assessment` anticipates.
     **Fixed**: carries `existing.get("current_adjudication")` forward
     when present and non-null.
  3. **Atomic-write test didn't exercise the new code path (test-quality
     gap)**: the original test patched `annotate.os.replace` globally,
     but the interruption actually fired inside
     `checkpoint.write_checkpoint` (called *before* the new merge/write
     path), so the test passed for the wrong reason and never verified
     the new append path's atomicity at all. **Fixed**: rewrote the test
     to call `merge_genre_sidecar` + `_write_json` directly, isolating
     exactly the write this item's own risk notes call out.
- Independently re-verified (Step 4, mandatory, done by this session
  directly): reproduced Finding 1 myself before accepting it - ran the
  exact two-call sequence against a fresh checkpoint dir and confirmed
  `assessments` grew from 1 to 2 while `backend.calls` stayed at 2 (zero
  new LLM calls), matching the subagent's claim exactly.
- Added 2 new regression tests (`test_checkpoint_hit_is_a_no_op_not_a_
  duplicate_append`, `test_merge_preserves_existing_current_
  adjudication`) covering the two real bugs, on top of the subagent's own
  fix to the pre-existing atomic-write test.
- No scope creep found (promote.py/adjudication UI/local-model source
  all untouched, matching this item's own Non-Goals).

# Validation

- `python3 -m pytest tests/analysis_tests/annotate_test.py -v` (own
  re-run after applying fixes): 25 passed (23 original + 2 new).
- `scripts/format --check --diff`, `scripts/lint`: clean (after
  re-pinning ruff/black - shared-env drift recurred mid-session).
- `scripts/test`: 1867 tests, OK.
- `lrh validate`: 191 pre-existing errors (unchanged baseline), 0
  attributable to this diff.

# Follow-up

- None. `/lrh-implement` Step 8 (commit, push, open PR) proceeds next
  regardless of this result, per this skill's own design.
