---
execution_id: 2026_08_26_03_31_18_WI_EVENT_0096
prompt_id: PROMPT(AD_HOC:WI_EVENT_0096)[2026-08-26T03:29:23+00:00]
work_item: AD_HOC
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/396
commit: 6ba44535
agent: claude_app
instruction_source: lcats/project/work_items/proposed/WI-EVENT-0096.md
session_transcript: claude-app:e8e46d5d-35d3-4ccc-9cba-137bd31bf3a5
created_at: 2026-08-26T03:31:18+00:00
---

# Summary

Created work item `WI-EVENT-0096`: a real-API measurement item that closes
`WI-EVENT-0033`'s one outstanding acceptance criterion - a live re-run to
measure whether its schema-hardening fix (PR #188) actually reduced the
65% (11/17) `parsing_error` segmentation-exclusion rate observed before
that fix. Requested via `/lrh-work-item` with the args "to accomplish this
final scope of work[,] the results of this run must be saved as data
somewhere (perhaps in the experiments directory if it's large enough, in
the experimental directory if not)" - a direct continuation of this
session's own prior discussion identifying WI-EVENT-0033's unmet criterion.

# Result

Created `lcats/project/work_items/proposed/WI-EVENT-0096.md` (type:
evaluation, `depends_on: [WI-EVENT-0033]`, related workstream
`WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY`). Opened PR #396 (branch
`xenotaur/audit/wi-event-0096`, commit `7f67db57`).

**Corrected in this PR's review round (two P1s from
`chatgpt-codex-connector`, both confirmed real before fixing):** the
initial draft wrongly claimed the original 17-story cohort's IDs were
never committed (an insufficiently broad search); they are, in
`experiments/03_cross_segment_relation_pilot/results/pilot_stories.jsonl`
(17 rows, 11 `parsing_error` exclusions, 5/5/2/5 genre split - a
byte-for-byte match to the audit). It also wrongly proposed comparing raw
`parsing_error` counts, which `llm_extractor.py`'s `tool_schema` branch
sets to `None` unconditionally - a 0%-by-construction tautology,
independently confirmed by reading that code directly. The WI was rewritten
to use the exact committed cohort (resolved to current `corpora/` paths)
via the already-built, already-reviewed
`check_segmentation_reliability.py` (PR #189), comparing the any-cause
exclusion rate instead. Two `copilot-pull-request-reviewer` findings
(`related_design`/`instruction_source` paths missing the `lcats/` prefix)
were also fixed in both the WI and this record.

# Validation

- `lrh validate` - 0 errors, 239 warnings (pre-existing baseline), rerun
  after the review-round rewrite

# Follow-up

- `session_transcript: pending` above should be updated to the durable
  session pointer once available.
- This item does not itself implement the measurement - `/lrh-execute
  WI-EVENT-0096` (or manual execution) is the next step, gated on
  `WI-EVENT-0033` per `depends_on` (already merged, so not a blocker) and
  on presenting a real-API cost estimate for explicit approval before any
  spend, per this item's own acceptance criteria.
- No workstream frontmatter update was made in this pass -
  `WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY.md`'s `work_items:` list
  currently lists only `WI-EVENT-0032`/`WI-EVENT-0033`/`WI-EVENT-0061`;
  offering to add `WI-EVENT-0096` to it is a candidate follow-up, not yet
  actioned pending user confirmation.
