---
execution_id: 2026_08_28_16_49_22_WI_RUNLOG_0083_REVIEW
prompt_id: PROMPT(AD_HOC:WI_RUNLOG_0083_REVIEW)[2026-08-28T16:46:44+00:00]
work_item: AD_HOC
status: landed
rerun_of: 2026_08_28_07_28_36_WI_RUNLOG_0083
pr: https://github.com/xenotaur/LCATS/pull/407
commit: 03f04c7f1e2d65cd78516a860c6628faf32f02d2
created_at: 2026-08-28T16:49:22+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/407
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-review-response https://github.com/xenotaur/LCATS/pull/407` —
addressed 2 review comments (1 Codex, 1 Copilot).

# Result

1. **P1, genuine data-integrity bug: `log_dir` was only validated
   against the canonical global `data/`/`corpora/` roots (via
   `run_log.RunLog`'s own guard), not against this specific call's
   actual `source_root`/`dest_root` arguments.** A `log_dir` nested
   under `source_root` (e.g. `source_root / "clean" / "logs"`) would
   have been silently wholesale-copied into the promoted destination
   corpus by `_copy_collection`'s own unfiltered `copytree`,
   contaminating a release with an operational log file. Added
   `_validate_log_dir_outside_promotion_roots()`, called before entering
   the `RunLog` scope, rejecting a `log_dir` equal to or nested under
   either root. Added 2 regression tests (source-nested, dest-nested).
2. **Allowlist config load happened before entering the `RunLog`
   scope**, so an unexpected failure there would never produce
   `run_start`/`run_aborted_unexpected` — contradicting the docstring's
   claim that the whole surveying phase is wrapped. Moved
   `specials.load_allowlist_config(...)` inside the `with` block, as the
   first statement. Added 1 regression test confirming a simulated
   config-load failure now produces exactly
   `["run_start", "run_aborted_unexpected"]`.

Both genuine and fixed; none skipped. Also ran `black --config
/dev/null` on `promote.py`, which needed one genuine reflow (a
too-long `if` condition in the new validation function) — applied.

# Validation

- `PYTHONPATH="$(pwd)/src:$(pwd)/tests:$PYTHONPATH" python3 -m unittest
  tests.analysis_tests.promote_test tests.gather_promote_e2e_test -v`
  — 79/79 pass (3 new regression tests).
- `black --check --config /dev/null src tests tools` (full-tree) /
  `ruff check --isolated src tests` — clean.
- `lrh validate` — exit 0, only pre-existing unrelated warnings.

# Follow-up

- Reminder: `session_transcript` should be confirmed/updated at closeout
  time if it differs from the live `CLAUDE_CODE_HOST_SESSION_ID`
  convention.
- Next: `/lrh-confirm-fixes` to verify the fixes against the current diff
  and resolve the review threads.
