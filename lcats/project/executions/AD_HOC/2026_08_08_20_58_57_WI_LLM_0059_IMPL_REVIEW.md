---
execution_id: 2026_08_08_20_58_57_WI_LLM_0059_IMPL_REVIEW
prompt_id: PROMPT(AD_HOC:WI_LLM_0059_IMPL_REVIEW)[2026-08-08T20:58:47+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/266
commit: 1a382b81
created_at: 2026-08-08T20:58:57+00:00
agent: claude_app
instruction_source: lrh request review_response https://github.com/xenotaur/LCATS/pull/266
session_transcript: pending
---

# Summary

Review-response round 2 for PR #266, same slug as round 1
(`wi-llm-0059-impl-review`) per the multi-round naming convention
(no round-number suffix).

# Result

Two more findings from the same first-push review batch (only the
secrets-import finding was addressed in round 1; these two were still
open):

- **P1 (Codex) - "Preserve segments before declaring Anthropic
  neutral"**: valid. `common/harness.py`'s `BenchmarkResult` only
  records `segment_count`, not the segments themselves, so round 1's
  "no regression" claim for the Anthropic leg had no content evidence
  behind it. Fixed: `run_frontier_paired.py` now calls
  `scene_analysis.make_segment_extractor()` directly (not
  `harness._run_segmentation_once()`) and persists each call's actual
  segment types/summaries to `results_frontier_paired_anthropic.json`.
  Re-ran the 3 Anthropic pairs (6 more real, billed calls - user
  explicitly authorized this re-spend given the finding's validity) with
  the new capture in place. This surfaced a real finding the count-only
  view had missed and mischaracterized as clean: 2 of 3
  modified-condition runs split the story's ending into an extra segment
  (baseline stayed at 4 all 3 times; modified produced 4, 5, 5) - the
  same split pattern both times, not one-off noise. Not a functional
  failure (every segment used a valid label and read coherently), but
  not a clean "neutral" result either. Corrected
  `PROP-ERW-LOCAL-MODEL-EVALUATION`'s Decision 3 update and
  `wi_llm_0059/README.md` to state this honestly instead of the
  overstated "no regression" claim from the first draft.
- **Codex - "add a backend/leg selection or resume option"**: valid.
  `run_frontier_paired.py` couldn't rerun just the OpenAI leg without
  redoing (and re-billing) the 3 Anthropic pairs first. Fixed: added
  `--legs {anthropic,openai,all}`, with each leg's results split into
  its own file (`results_frontier_paired_anthropic.json`,
  `results_frontier_paired_openai.json`) that a single-leg rerun loads
  instead of regenerating. Recovered the original OpenAI pair's data
  (previously only inside the combined file, since the split didn't
  exist yet when it was first run) into its own file retroactively.

Also hit and fixed an unrelated environment issue mid-round: the shared
conda env's editable `lcats` install had drifted to point at a different
worktree (`worldcon-fast-path-annotation-271d6d`) between this PR's first
push and this round - `./scripts/test` showed 7 failures/1 error that
disappeared entirely once `pip install -e ".[dev]"` was re-run from this
worktree, confirming the failures were an install-drift artifact, not a
real regression (per this session's standing convention: verify
`module.__file__` before trusting a test run in a shared-env worktree).

Fixes committed as `1a382b81`, pushed directly to the PR branch.

# Validation

- `black --check --diff` / `ruff check` on the modified
  `run_frontier_paired.py` - clean.
- `python -c "import ast; ast.parse(...)"` - syntax valid.
- `python -c "import lcats; print(lcats.__file__)"` - confirmed points to
  this worktree after the reinstall.
- `./scripts/test` - 1664 passed (after the editable-install fix).
- `lrh validate` - 0 errors, 123 warnings (all pre-existing).
- 6 more real, billed Anthropic calls (3 re-run pairs) - user-authorized
  given the P1 finding's validity and the resulting cost.

# Follow-up

- None outstanding from this round - both findings fixed. Next:
  `/lrh-confirm-fixes` to verify and resolve all 4 review threads
  (this round's 2, round 1's already-fixed secrets-import finding, and
  Copilot's formatting finding - a confirmed false positive, to be
  skipped with documented rationale at the confirm-fixes gate rather
  than resolved as fixed).
