---
execution_id: 2026_08_08_05_05_04_WI_LLM_0059
prompt_id: PROMPT(AD_HOC:WI_LLM_0059)[2026-08-08T04:50:48+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/260
commit: 1dbbd5d2e99d170bc4647f4139b3efebad827906
created_at: 2026-08-08T05:05:04+00:00
agent: claude_app
instruction_source: lcats/project/work_items/proposed/WI-LLM-0059.md
session_transcript: pending
---

# Summary

Created `WI-LLM-0059` via `/lrh-work-item`, a follow-on investigation to
`WI-LLM-0051` (resolved, PR #254): whether appending WI-LLM-0051's tested
system-prompt reminder to the *production* `SCENE_SEQUEL_SYSTEM_PROMPT`
(`scene_analysis.py`, shared across every `LLMBackend`) - not just the
benchmark harness's own retry-only copy - improves local-model
segmentation reliability without regressing frontier-model (Claude/GPT)
output quality or latency.

# Result

Wrote `project/work_items/proposed/WI-LLM-0059.md` (type `investigation`,
`status: proposed`, `depends_on: [WI-LLM-0051]`) scoping the
investigation: (1) real segmentation calls against `ollama_qwen3_8b`
(and `ollama_qwen3_30b_a3b` if time permits) with the reminder appended
to the real prompt via `_run_segmentation_once()`'s existing
`system_prompt_suffix` parameter, reusing WI-LLM-0051's test vehicle
rather than editing production code speculatively; (2) a real
`claude-opus-4-8` call with the identical modified prompt on the same
story, compared against an unmodified-prompt baseline, as a hard gate
against frontier-path regression before any production edit; (3) a
conditional production edit to `scene_analysis.py`'s
`SCENE_SEQUEL_SYSTEM_PROMPT` with covering tests if the evidence
supports it, or a documented "no change warranted" verdict in
`PROP-ERW-LOCAL-MODEL-EVALUATION` if not - mirroring WI-LLM-0051's
negative-result-tolerant framing. `related_workstreams` left empty,
matching WI-LLM-0050/0051 (this proposal lineage has never been linked
to a workstream). `depends_on` was left one-directional (no edit to
WI-LLM-0051's own frontmatter) since WI-LLM-0051 is already resolved,
not a live proposed/active item an implementer could pick prematurely.
Opened PR #260 with the work item as its only change.

# Validation

- `lrh validate` - 0 errors; only pre-existing warnings plus the same
  `OWNER_ROLE_INSUFFICIENT`/`OWNER_NOT_IN_CONTRIBUTORS` warnings every
  other work item in this lineage already carries (`owner: unassigned`).
- `lrh prompt check-execution --slug wi-llm-0059 --work-item AD_HOC
  --project-root .` - no prior record found before minting.

# Follow-up

- This PR only adds the planning artifact - the actual investigation
  (real Ollama + Claude segmentation calls, verdict, and conditional
  production edit) is deferred to whoever executes `WI-LLM-0059` next
  (e.g. via `/lrh-execute WI-LLM-0059`).
- `session_transcript: pending` above should be updated to
  `claude-app:<host-uuid-stem>` after this session ends.
