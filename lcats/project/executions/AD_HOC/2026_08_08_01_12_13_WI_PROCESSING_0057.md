---
execution_id: 2026_08_08_01_12_13_WI_PROCESSING_0057
prompt_id: PROMPT(AD_HOC:WI_PROCESSING_0057)[2026-08-08T01:11:53+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/250
commit: f612640b6733aa5cdb4f5216cd60ceb96b1e839f
created_at: 2026-08-08T01:12:13+00:00
agent: claude_app
instruction_source: project/design/backlog.md
session_transcript: claude-app:693d6013-727b-422d-a378-5dc4242d3076
---

# Summary

Created `WI-PROCESSING-0057`: guard two real unguarded
`pathlib.Path.resolve()` bugs found by an audit of all 14 other
unguarded call sites left over from `WI-ASSESS-0050`'s own backlog
follow-up.

# Result

- Ran the audit: read every remaining unguarded `.resolve()` call site
  (`promote.py`, `processing.py`, `output.py`, `utils/paths.py`,
  `utils/checkpoint.py`) and its call chain. Confirmed 10 sites
  (`promote.py`'s `_validate_distinct_roots`, `checkpoint.py`'s
  `_protected_roots`/`_check_working_root_allowed`, `utils/paths.py`'s
  `find_pyproject_root`) are pre-destructive/bootstrap safety checks
  where crashing on an unresolvable path is correct, intentional
  behavior -- explicitly left alone as this WI's Non-Goals.
- Found 2 real bugs: `output.story_dir_value` (the exact pattern
  `assess_story`'s own fix was modeled on, never itself guarded,
  reachable from `cli.py`'s `run_survey` per-file loop which has no
  exception handling at all) and `processing.py`'s
  `process_files`/`process_file` (an eager per-file resolve step that
  runs *before* `process_file`'s own per-file fault-isolation
  try/except, so one bad path aborts an entire batch before any
  processing starts).
- Ran the prior-art check: duplication search found `output_test.py`
  has `TestStoryDirValue` coverage but no resolve-failure test, and
  `processing.py` has no dedicated test file at all; demand search
  found the matching `backlog.md` entry this WI resolves.
- Confirmed plan with the user before writing.
- Wrote `project/work_items/proposed/WI-PROCESSING-0057.md`.
- Opened [PR #250](https://github.com/xenotaur/LCATS/pull/250).

# Validation

- `lrh validate` -- 0 errors, 113 warnings (pre-existing `owner:
  unassigned` pattern shared by every other work item in this repo).

# Follow-up

- Offer (per the prior-art demand-search recommendation, not
  auto-closed): once this PR is confirmed/merged, remove or mark
  resolved the matching `project/design/backlog.md` entry ("Unguarded
  `pathlib.Path.resolve()` calls could crash callers on filesystem
  errors").
- `status` is `in_progress`; update to `landed` via `/lrh-closeout`
  after PR #250 merges. `WI-PROCESSING-0057` itself stays `proposed`
  until its implementation lands as a separate PR.
