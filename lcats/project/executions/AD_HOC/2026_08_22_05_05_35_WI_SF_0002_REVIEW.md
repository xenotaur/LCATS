---
execution_id: 2026_08_22_05_05_35_WI_SF_0002_REVIEW
prompt_id: PROMPT(AD_HOC:WI_SF_0002_REVIEW)[2026-08-22T04:58:41+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_22_02_30_00_WI_SF_0002
pr: https://github.com/xenotaur/LCATS/pull/344
commit: 21e2fdf3269e73d3a91dd1e08835d1a96fcb54c3
agent: codex_app
instruction_source: https://github.com/xenotaur/LCATS/pull/344
session_transcript: codex-app:01a02338-d9c7-7313-8ed5-fb9c1643bef1
created_at: 2026-08-22T05:05:35+00:00
---

# Summary

Addressed review feedback on PR #344 for `WI-SF-0002`.

# Result

Fixed all five returned review comments:

- Renamed the focused test file from `test_preparation.py` to
  `preparation_test.py` so the repository's `*_test.py` discovery pattern
  runs the new preparation tests.
- Reworked adaptive chunk planning so overlap paragraphs are included only
  while the projected chunk remains within `chunk_max_chars` when possible.
- Revised section-boundary selection to inspect all valid candidate core ends
  around the target size and prefer a nearby boundary instead of stopping at
  the first target-crossing paragraph.
- Added a type check that rejects non-object JSON story files before passing
  data into `story_from_record`.
- Changed chunk-size projections to use paragraph source spans instead of
  paragraph text lengths plus synthetic separator assumptions.

Added no-cost regression tests for configured max-size overlap trimming,
boundary preference before the target size, and non-object story JSON.

# Validation

- `cd lcats && PATH=/Users/centaur/anaconda3/bin:$PATH scripts/format --check --diff`
  passed: 198 files would be left unchanged.
- `cd lcats && PATH=/Users/centaur/anaconda3/bin:$PATH scripts/lint`
  passed with Ruff and Black checks clean. `scripts/version tools` reported
  Homebrew wrapper versions for bare `ruff`/`black`; `python -m black --version`
  and `python -m ruff --version` confirmed the pinned package versions used by
  the PATH-adjusted checks: Black 25.11.0 and Ruff 0.15.0.
- `cd lcats && PYTHONPATH=src python -m unittest tests.analysis_tests.science_fiction.preparation_test`
  passed: 11 tests OK.
- `cd lcats && PYTHONPATH=src python -m unittest discover -s tests/analysis_tests/science_fiction -p '*_test.py'`
  passed: 11 tests OK.
- `cd lcats && scripts/test` passed: 1833 tests OK.
- `lrh validate --project-dir lcats/project` passed with 0 errors and 166
  inherited warnings.
- `git diff --check` passed.

# Follow-up

Continue `/lrh-land` for PR #344, including confirm-fixes and merge gating.
