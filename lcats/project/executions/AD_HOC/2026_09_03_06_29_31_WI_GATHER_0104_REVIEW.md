---
execution_id: 2026_09_03_06_29_31_WI_GATHER_0104_REVIEW
prompt_id: PROMPT(AD_HOC:WI_GATHER_0104_REVIEW)[2026-09-03T06:29:16+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_09_02_19_36_53_WI_GATHER_0104
pr: https://github.com/xenotaur/LCATS/pull/424
commit: 
created_at: 2026-09-03T06:29:31+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/424
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-review-response https://github.com/xenotaur/LCATS/pull/424` —
addressed 5 review comments (3 Copilot, 2 Codex). This round exists
because the earlier empty-thread check (in the prior `_CONFIRM` record,
`2026_09_02_19_45_49`) raced a real Copilot/Codex review that posted on
the implementation commit moments after that check ran but before the
`_CONFIRM` record was pushed — a genuine review had already landed and
was missed by timing, not a case of nothing to review.

# Result

1. **Genuine, Copilot: `gutenberg_url=None` with no validation.** When
   both `gutenberg_url` and `entry_url` are omitted, `url` silently
   resolves to `None`, propagating into `DataGatherer.download()` for an
   unclear later failure. Added an explicit `ValueError` at the top of
   `gather()` when neither is given, plus a test.
2. **Genuine, Copilot: `extraction_strategy` failure message referenced
   heading search.** The `ValueError` on extraction failure always said
   "given {start_heading_text}" even when `extraction_strategy` (not
   heading search) was in use — for lovecraft, `start_heading_text` is
   an empty string, making the message actively misleading. Split into
   two branches with a strategy-appropriate message each.
3. **Genuine, Copilot: order-sensitive test assertion.**
   `test_entry_url_overrides_shared_gutenberg_url_per_entry` compared a
   list of called URLs against `per_entry_urls.values()`, implicitly
   depending on dict-insertion order matching `EXAMPLE_HEADINGS` order.
   Rewrote to assert the actual filename→URL mapping instead.
4. **Genuine, Codex P2: extension params weren't keyword-only.** Since
   they're inserted between the previously-last-defaulted param and
   `verbose`/`log_dir`, a hypothetical positional caller passing those
   old params positionally would silently misassign into the new slots.
   No current caller does this (independently verified during the diff-
   mode self-review), but it's a real latent API-safety gap. Made
   `extraction_strategy`, `entry_url`, `name_source`, `verbose`, and
   `log_dir` keyword-only (`*` inserted after `paragraph_finder`).
5. **Genuine, Codex P1: dropped JSON-serialization invariant.** The
   retired `lovecraft.gatherer.TestCreateDownloadCallback` had a
   `test_callback_metadata_is_json_serializable` test; my replacement
   tests didn't carry that invariant forward, a regression against
   `tests/AGENTS.md:24-27`'s explicit "for serializers/extractors, add
   an invariant test such as 'result can be json.dumps'd'" rule.
   Restored it as an assertion inside the end-to-end wiring test.

All 5 genuine and fixed; none skipped.

# Validation

- Full test suite (`PYTHONPATH`-corrected) — 2243/2243 pass (1 more than
  before, for the new URL-validation test).
- `scripts/format --check --diff` / `scripts/lint` — clean (one
  `scripts/format` auto-fix applied).
- `lrh validate` — exit 0.

# Follow-up

- Next: `/lrh-confirm-fixes` to verify these fixes against the current
  diff and resolve the review threads — this time waiting for a genuine
  affirmative REVIEW-LANDED signal against the resulting `_CONFIRM`
  commit before treating anything as green.
