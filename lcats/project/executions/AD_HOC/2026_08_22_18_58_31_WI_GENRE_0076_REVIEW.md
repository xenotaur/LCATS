---
execution_id: 2026_08_22_18_58_31_WI_GENRE_0076_REVIEW
prompt_id: PROMPT(AD_HOC:WI_GENRE_0076_REVIEW)[2026-08-22T18:57:58+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_22_18_38_43_WI_GENRE_0076
pr: https://github.com/xenotaur/LCATS/pull/357
commit: aa053ecf651708ca6064aeb61b745f271c74e758
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/357
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-22T18:58:31+00:00
---

# Summary

Review-response round for PR #357 (`WI-GENRE-0076`). The PR's automatic
first-push review surfaced 3 real P1 findings from
`chatgpt-codex-connector`.

# Result

- **Failure path deletes valid v1 ledger (fixed)**: `annotate_story()`'s
  failure branch unconditionally called `_remove_if_exists()` on
  `genre.json`, deleting an existing, independently-valid v1 ledger just
  because a later, unrelated recompute failed. Now checks whether the
  existing file is already a valid v1 record before removing it - a
  valid ledger survives a later failure untouched; a legacy/corrupt/
  missing file is still cleaned up as before (preserving the original
  PR #241 intent for that narrower case).
- **Checkpoint-hit no-op skips legacy migration (fixed)**: the no-op
  check added in this PR's own diff-mode self-review only tested
  `is_file()`, so a pre-`WI-GENRE-0076` legacy sidecar re-annotated under
  an unchanged config was silently left un-migrated on a cache hit. Now
  also requires the existing file to already validate as v1 before
  treating a cache hit as a no-op.
- **v1 output incompatible with `lcats promote`'s wholesale gate (fixed,
  scope amended)**: `promote.py`'s `_validate_sidecars()` required a
  top-level `detected_genre` key unconditionally, which a v1-shaped
  `genre.json` never carries - every sidecar this item's own writer
  produces would be rejected by `lcats promote <collection>`'s wholesale
  path. `WI-GENRE-0076`'s `forbidden_actions` originally barred
  `modify_lcats_promote`; amended to the narrower
  `change_promote_wholesale_replacement_default_behavior` with explicit
  human authorization (`WI-GENRE-0076.md`, `related_design` and
  `artifacts_expected` updated to reflect the amended scope). Fixed by
  checking a non-legacy-flat `genre.json` via
  `genre_sidecar.validate_sidecar()` instead of the legacy check;
  `scenes.json`'s check and the actual wholesale copy/replace mechanism
  are unaffected.
- 7 new/updated tests across `annotate_test.py`/`promote_test.py`,
  including 2 pre-existing tests whose own premise the fixes correct
  (`test_stale_genre_sidecar_removed_when_recompute_fails` split into
  `test_valid_v1_sidecar_survives_a_failed_recompute` +
  `test_stale_legacy_sidecar_removed_when_recompute_fails`;
  `test_missing_required_key_sidecar_blocks` updated for the new
  v1-validation error message, with a new
  `test_missing_required_key_scenes_sidecar_blocks` added to keep
  scenes.json's own legacy-check coverage explicit).

# Validation

- `scripts/version tools`: repaired ruff/black pin drift before trusting
  any result.
- `scripts/format --check --diff`, `scripts/lint`: clean.
- `python3 -m pytest tests/analysis_tests/annotate_test.py tests/analysis_tests/promote_test.py -v`:
  70 passed.
- `scripts/test`: 1871 tests, OK.
- `lrh validate`: 191 pre-existing errors (unchanged baseline), 0
  attributable to this PR.
- Pushed directly: commit to the existing open PR branch
  `xenotaur/feat/wi-genre-0076`.

# Follow-up

- None outstanding from this round. Proceeding to `/lrh-confirm-fixes`
  next to verify the fixes against the current diff and resolve the
  review threads.
