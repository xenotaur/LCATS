---
execution_id: 2026_08_05_17_15_11_WI_ASSESS_0031
prompt_id: PROMPT(WI-ASSESS-0031:WI_ASSESS_0031)[2026-08-05T17:01:59+00:00]
work_item: WI-ASSESS-0031
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/224
commit: a5a6e5e6
agent: claude_app
instruction_source: project/work_items/proposed/WI-ASSESS-0031.md
session_transcript: pending
created_at: 2026-08-05T17:15:11+00:00
---

# Summary

Implement `WI-ASSESS-0031`: extend `assess.py`'s `VALID_GENRES` from 4 to the 8 genres confirmed as the real Worldcon 2026 target, and add a new open `secondary_genre` field so non-priority categories stay representable.

# Result

- `VALID_GENRES` extended to `("science fiction", "horror", "humor", "western", "romance", "mystery", "fantasy", "adventure")` in `src/lcats/analysis/corpus/assess.py`, with matching `_GENRE_DEFINITIONS` entries for the 4 new genres.
- All "four genres"/"four target genres"/"four targets" wording in the classifier's schema descriptions and both system prompts (`DETECT_SYSTEM_PROMPT`, `SYSTEM_PROMPT_TEMPLATE`) updated to "eight" — verified via `grep -n "four" assess.py` returning zero matches.
- Added a new open (non-enum) `secondary_genre` string field to `ASSESSMENT_TOOL`'s schema, `AssessmentResult` (default `""`), and `assess_story()`'s result-building code — populated in both detect and lens mode regardless of `genre_verdict`, distinct from `genre_suggestion` (which stays schema-gated to wrong/disputed lens results only, per its unchanged description). Both system prompts instruct the model to populate it unconditionally.
- Wired `secondary_genre` through `assess_cli.py`'s `TSV_COLUMNS`, `_result_to_tsv_row()`, and `_write_human()`. Confirmed the `--genre` epilog examples need no changes — they use genre names (`science fiction`, `horror`, `western`, `romance`) that remain valid members of the expanded `VALID_GENRES`.
- Updated `tests/analysis_tests/assess_test.py`: `_SAMPLE_TOOL_RESULT` fixture gained `secondary_genre`; added `test_secondary_genre_passed_through`, `test_secondary_genre_defaults_empty`, `test_new_genre_accepted_in_lens_mode` (exercises `mystery` end-to-end), and a `TestValidGenres` class (8-entry count, new-genre membership, retained-genre membership, `detected_genre` enum parity with `VALID_GENRES`, schema field presence).
- Checked for other in-repo consumers of `VALID_GENRES`/`detected_genre` with a hardcoded 4-genre assumption: `experiments/02_llm_backend_comparison/compare_results.py` reads `genre_verdict` by key with no allowlist, unaffected. `experiments/03_cross_segment_relation_pilot/run_pilot.py` and `WI-EVENT-0030.md` do still hardcode the old 4-genre framing, but re-scoping those is this WI's own explicit non-goal (Gap 3 in the reconciliation doc, a separate follow-up item) — correctly left untouched.
- Fixed `WI-ASSESS-0031.md`'s own stale `lcats/lcats/...` path citations (pre-src-layout-move, from `WI-PACKAGING-0032`) to `lcats/src/lcats/...`, and corrected its `assess.py` line-number citations (`149,153,181` → `158,162,175,190`; `85-91` → `88-95`), which had drifted since the WI was written — the file gained a `strict_tool_schema()` wrapper from later strict-schema work.
- **Bucket-layout compatibility confirmed live, not just by code reading**: `lcats assess ../corpora/sherlock --dry-run` correctly discovered and previewed real `<story>/story.json` bucket files (e.g. `corpora/sherlock/beryl_coronet/story.json`) — `assess_cli.py`'s existing `discovery.find_json_files()` call needed zero changes, exactly as predicted by the earlier investigation session that paused this work item pending the bucket/checkpointing migrations.
- Diff-mode `/lrh-self-review` pass (cold-context subagent, execution record `2026_08_05_17_13_39_WI_ASSESS_0031_SELFREVIEW`) found no issues before this PR's first push; its top finding was independently re-verified by this session directly (see that record's own Validation section).

# Validation

- `scripts/format --check --diff` — clean, 179 files unchanged.
- `scripts/lint` — ruff and black both clean.
- `scripts/test` — 1601 tests, OK.
- `lrh validate` — 0 errors, 71 pre-existing warnings (unrelated to this change).
- `lcats assess tests/ --dry-run` and `lcats assess tests/ --genre mystery --dry-run` — both exit 0; separately confirmed `--genre not-a-real-genre` correctly errors (exit 2) listing all 8 genres, proving the CLI's argparse `choices` surface is live-wired to the new `VALID_GENRES`.
- `lcats assess ../corpora/sherlock --dry-run` — confirms real bucket-layout story discovery end to end (see Result).
- Editable install was pointing at a different worktree at session start (`scripts/develop` re-run; verified via `python -c "import lcats; print(lcats.__file__)"` before and after) — all validation above ran against this worktree's actual code, not stale cached state.

# Follow-up

- `session_transcript: pending` should be updated to `claude-app:<session-id>` after this session ends.
- Gap 2 (current-classifier full-corpus genre survey) and Gap 3 (re-scope `WI-EVENT-0030`'s stratified pilot to 8 genres) remain unscoped as formal work items, per the reconciliation design doc's own recommendation — both still need their own cost/latency estimates before being run at any real scale.
