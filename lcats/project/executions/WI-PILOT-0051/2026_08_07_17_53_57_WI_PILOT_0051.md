---
execution_id: 2026_08_07_17_53_57_WI_PILOT_0051
prompt_id: PROMPT(WI-PILOT-0051:WI_PILOT_0051)[2026-08-07T16:45:06+00:00]
work_item: WI-PILOT-0051
status: in_progress
rerun_of:
pr: https://github.com/xenotaur/LCATS/pull/244
commit:
agent: claude_app
instruction_source: project/work_items/proposed/WI-PILOT-0051.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-07T17:53:57+00:00
---

# Summary

Implemented `WI-PILOT-0051`: added `--story`/`--story-list` targeted
test harness flags to `run_pilot.py`, a committed fixture set, and
per-stage cost/timing recording for the segmentation stage (closing
backlog P2 for that stage), via `/lrh-execute`'s inlined
`/lrh-implement`.

# Result

- `run_pilot.py`: new `--story <collection/name>`, `--story-list
  [FILE]`, `--genre` flags bypass `build_stratified_sample`'s
  200-candidate genre-detect scan entirely. `--story` requires an
  explicit `--genre`; `--story-list`'s manifest carries genre
  per-entry; no implicit genre-detect call is ever made in targeted
  mode (deliberately, per the proposal's Decision 2(d) and this WI's
  own Risk Notes against silently defaulting).
- `--story-list` given with no argument defaults to a new committed
  fixture set (`fixtures/`, 2 real short public-domain stories from
  `corpora/mass_quantities/`) - the zero-config default within targeted
  mode only; the script's own no-argument invocation is unchanged.
- `_segment_story`/`_segment_story_cached` now return usage info; a
  fresh (non-cached) segmentation call is recorded as a
  `PassUsage`-shaped row in `pilot_usage.jsonl`; a cache-hit correctly
  emits none (no double-counting, verified by test).
- Extracted the per-story loop (previously inline in `main()`) into a
  shared `_run_stories()` helper, used by both the existing full-sample
  path and the new targeted path, so the `FatalPilotError`/generic-
  exception handling logic exists in exactly one place.
- Added `experiments/03_cross_segment_relation_pilot/fixtures/`
  (`README.md`, `manifest.txt`, 2 story.json files) and 14 new tests in
  `run_pilot_test.py` covering segmentation-usage recording, path/
  manifest resolution, and end-to-end `--story`/`--story-list` runs
  through the real `main()` CLI path (not mocked internals).
- Also fixed a stale, unrelated `project/design/backlog.md` entry
  (ERW pipeline audit's Category E - already promoted to two
  proposals) noticed and flagged for a backlog entry at this session's
  `/lrh-execute` kickoff; small enough to bundle rather than a separate
  PR.
- Ran `/lrh-implement` Step 7.5 (`/lrh-self-review` diff-mode, cold-
  context independent subagent + my own mandatory re-verification of
  its top finding) before the first push - found no correctness issues,
  2 minor doc-completeness gaps fixed (see the `_SELFREVIEW` execution
  record).

# Validation

- `scripts/version tools`, `scripts/format --check --diff`,
  `scripts/lint`, `scripts/test` (from `lcats/`) - 1632 tests pass, 0
  failures.
- `python -m unittest experiments/03_cross_segment_relation_pilot/run_pilot_test.py`
  - 23 tests, all pass.
- `lrh validate` - 0 errors, 109 warnings (unchanged baseline; no new
  warnings tied to this WI's files).
- Manual smoke tests (`--dry-run`, zero real API cost): `--story-list`
  (fixture default, both stories), `--story fixtures/king_of_the_hill
  --genre "science fiction"` (single story), and the unmodified
  full-sample path (`--data-dir corpora --sample-size 1`) all verified
  end to end.
- Pre-flight: caught and fixed a stale editable install (`lcats.__file__`
  pointed at a different worktree) before trusting the first `scripts/test`
  run - re-ran `scripts/develop`, confirmed `lcats.__file__` now points
  at this worktree, then re-ran the full suite clean.
- `gh pr diff 244 --name-only` confirmed only the 9 intended files in
  the PR diff.

# Follow-up

- The WI-numbering collision noticed at `/lrh-execute`'s kickoff (four
  different `WI-*-0051` items across concurrent sessions) was flagged
  as its own separate `project/design/backlog.md` entry, not fixed here
  - does not block this WI.
- Next: `/lrh-review-response`/`/lrh-confirm-fixes` for PR #244's
  review round (using independent subagent self-review, not GitHub bot
  retriggers, per the standing quota-conservation policy), then merge
  and closeout.
