---
execution_id: 2026_09_23_18_47_11_WI_VISUALIZE_0095
prompt_id: PROMPT(WI-VISUALIZE-0095:WI_VISUALIZE_0095)[2026-09-23T18:17:41+00:00]
work_item: WI-VISUALIZE-0095
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/445
commit: f166f7f42a246bd6c38ad1f6b3778e9978b5a5ca
agent: claude_code
instruction_source: project/work_items/proposed/WI-VISUALIZE-0095.md
session_transcript: claude-app:155c7eed-1d58-47ff-b83e-0cf2570a7b6f
created_at: 2026-09-23T18:47:11+00:00
---

# Summary

Implement `WI-VISUALIZE-0095` through `/lrh-execute`: turn the PR #442 N-way
genre-deviation spike into a supported, reusable aligned multi-subset
comparison facility. It includes a "kabob" rendering preset built from
independent render-spec fields.

# Result

- **Analysis** (`src/lcats/visualize/comparison.py`): added
  - direct and complement panel modes;
  - `none`, `common`, and `per_panel_complement` reference policies;
  - per-cell reference provenance;
  - the `max_panel_value` vocabulary and ordering;
  - a public `validate_nway_spec`;
  - a v2 manifest with a universe fingerprint, references, all-pairs
    `panel_overlaps` and `base_overlaps`, verified `complements`,
    `membership` (`partition_claim: false`), `commensurability`, and
    empty-panel/empty-reference warnings.
- **Rendering** (`src/lcats/visualize/rendering.py`): added
  - `NWayRenderSpec`, whose enums cover the preset (standard/kabob),
    reference direction, term-label placement, hatching, legend, row guides,
    extrema scope, scale policy, and max columns;
  - a deterministic `NWayRenderPlan`;
  - wrapped bands with aligned columns, labeled independent scales, compact
    ticks, and hatched legend swatches.
- **Outputs**: a new `src/lcats/visualize/nway_outputs.py` writes figures,
  a long-form CSV, and a manifest with layout decisions and SHA-256 output
  hashes. Figure metadata is deterministic.
- **CLI**: `lcats visualize compare-many`.
- **Experiment**: the wrapper is now thin and generated five checked
  real-data examples under
  `experiments/08_visualize_dogfood/figures/nway_genre_deviation/`. The
  PR #442 artifacts are preserved unchanged.
- **Docs**: `docs/how-to/run-visualize.md`, `docs/reference/cli-commands.md`,
  and `experiments/08_visualize_dogfood/README.md`.

The prior-art check was present in the WI, and readiness was
`prompt_ready: yes`. The diff-mode self-review record is
`project/executions/AD_HOC/2026_09_23_18_45_40_WI_VISUALIZE_0095_SELFREVIEW.md`.

# Validation

- `scripts/version tools`: black 25.11.0 and ruff 0.15.0, matching the CI
  pins.
- `scripts/format --check --diff`: clean.
- `scripts/lint`: clean.
- `scripts/test`: 2341 tests, OK.
- `lrh validate`: 0 errors.
- Tests were run with the `LCATS` conda env and `PYTHONPATH=<worktree>/lcats/src`,
  because the editable install points to a sibling checkout.
- Numeric verification of all 5 real-data examples, recomputed independently
  from the source manifest JSONL: complements equal `U - S`, overlap counts
  are correct, shared-scale limits are identical, and every hash matches.
  One value was also recomputed from raw text and matched exactly.
- Manual PNG/SVG/PDF review at paper size (7 in) and presentation size
  (13.33 x 7.5 in), including grayscale and a 3-band wrapping case.

# Follow-up

None identified.
