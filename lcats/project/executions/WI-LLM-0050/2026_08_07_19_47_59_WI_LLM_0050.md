---
execution_id: 2026_08_07_19_47_59_WI_LLM_0050
prompt_id: PROMPT(WI-LLM-0050:WI_LLM_0050)[2026-08-07T18:50:19+00:00]
work_item: WI-LLM-0050
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/249
commit: 46491cc0104e95a8f3d90eb6ccee1b9a4f6a1cea
created_at: 2026-08-07T19:47:59+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0050.md
session_transcript: claude-app:6d988910-ee4a-4ccc-af0b-2fb13d91ddc5
---

# Summary

Implement WI-LLM-0050 via `/lrh-execute` (which inlines `/lrh-implement`
then `/lrh-land`) - extend
`lcats/experimental/model_comparison/common/harness.py` to cover the
genre-detection and scene/sequel-segmentation stages, and run at least
one local candidate against them, producing the evidence
`PROP-ERW-LOCAL-MODEL-EVALUATION`'s hybrid-pipeline hypothesis needed to
move past "plausible but unproven."

# Result

Added `run_genre_detection()` and `run_segmentation()` to
`common/harness.py`, reusing the real production call paths
(`lcats.analysis.corpus.assess.assess_story()` in detect mode,
`lcats.analysis.scene_analysis.make_segment_extractor()`), not synthetic
schemas - matching `run_entity_extraction()`'s existing pattern. Added an
optional `temperature` parameter to `assess_story()` (previously
hardcoded to 0.2), backward compatible with both existing callers, needed
to run genre detection at Qwen3's own documented sampling temperature.
Generalized `harness.save_result()` with an optional `filename` parameter
(default unchanged) so multi-stage candidates don't overwrite each
other's results.

Ran `ollama_qwen3_8b` twice (real, not simulated) against each new stage,
against the whole sample story
(`corpora/sherlock/five_orange_pips/story.json`), `temperature=0.6`:

- **Genre detection: 2/2 succeeded.** Correctly detected "mystery" both
  runs, at 40-96s latency.
- **Segmentation: 2/2 failed.** Both runs came back `finish_reason='stop'`
  with **no tool call at all** - despite the model's free-text response
  containing a fully-formed, schema-conformant JSON object matching
  `record_segments`'s schema almost exactly. This reproduces the residual
  Ollama `tool_choice` forced-function-name gap
  `PROP-ERW-LOCAL-MODEL-EVALUATION`'s Decision 3 already flagged as an
  open risk (and `WI-LLM-0051` exists to investigate), directly on a
  second, larger-schema stage - not just theorized.

Updated `PROP-ERW-LOCAL-MODEL-EVALUATION`'s Decision 3 with an explicit,
no-longer-open hybrid-pipeline verdict: genre detection is hybrid-viable
for this model, segmentation is not (yet, pending `WI-LLM-0051`'s
`tool_choice` investigation). Marked the Implementation Plan's follow-on
item #2 done with the real finding, matching the pattern used for
`WI-LLM-0049`'s landing elsewhere in the same file. Updated both
`model_comparison/README.md` and `ollama_qwen3_8b/README.md` with the new
stage coverage and real results tables.

Ran a diff-mode `/lrh-self-review` pass before the PR's first push (cold-
context subagent, full diff against `origin/main` plus WI-LLM-0050's
requirements) per this session's standing guidance to use self-review
rather than manually retriggering GitHub bots (Codex/Copilot near-
exhausted monthly quota). No hard defects found; independently traced the
observed segmentation failure through `openai_backend.py`'s tool-call
validation and `JSONPromptExtractor.extract()`'s exception path to
confirm it's correctly classified as `api_error`/`success=False`, not a
silent false-success. One stale docstring (`load_sample_story()` still
said "not used as direct benchmark input", no longer true) fixed before
push.

`origin/main` had advanced one commit (an unrelated concurrent WI-ASSESS-
0050 closeout) between branch creation and this push; rebased cleanly
before pushing so the PR diff stays limited to this work item's actual
changes.

# Validation

- `pip install -q "ruff==0.15.0" "black==25.11.0"` - re-pinned twice this
  session after ambient drift (a recurring environment issue, not unique
  to this run).
- `python -m pytest` - 1641 passed.
- `black --check --diff` / `ruff check` (CI-pinned versions) - clean.
- `lrh validate` - 0 errors, pre-existing warnings only.
- 2 real `benchmark_genre.py` runs, 2 real `benchmark_segmentation.py`
  runs (not simulated), all 4 committed as evidence regardless of
  outcome (including the 2 genuine segmentation failures).

# Follow-up

- `WI-LLM-0051` (already filed) should treat this run's segmentation
  failures as direct motivating evidence, not just theoretical risk, when
  investigating the `tool_choice` gap.
- Consider whether a future item should test segmentation against the
  `anthropic_opus` baseline for a same-stage frontier comparison point -
  out of this WI's scope (non-goal: "does not add new local-model
  candidates").
