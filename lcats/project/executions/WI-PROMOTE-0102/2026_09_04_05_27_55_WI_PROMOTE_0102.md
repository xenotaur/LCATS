---
execution_id: 2026_09_04_05_27_55_WI_PROMOTE_0102
prompt_id: PROMPT(WI-PROMOTE-0102:WI_PROMOTE_0102)[2026-09-04T05:16:45+00:00]
work_item: WI-PROMOTE-0102
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/427
commit: 4a41604487ee4fea2a8cc1d79a059174b52472f1
agent: claude_app
instruction_source: project/work_items/proposed/WI-PROMOTE-0102.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-09-04T05:27:55+00:00
---

# Summary

Implemented `WI-PROMOTE-0102`: investigated whether `promote.py`'s two
remaining direct `genre_sidecar` imports can be routed through the
`sidecar_validators` registry without a behavior change, or whether the
governing exit-criterion/acceptance-criterion wording should be narrowed
instead.

# Result

- Created `project/design/promote-genre-sidecar-import-assessment.md`,
  documenting: the origin of both usages (`WI-GENRE-0075`/`0076`, PRs
  #350/#357); an accurate per-usage account of what `WI-PROMOTE-0097`
  (PR #405) did to each (`_validate_sidecars` untouched,
  `_promote_sidecar_records`'s guard restructured but behaviorally
  preserved); that `sidecar_validators.py`'s own docstring already
  documents `replace` mode as never validating through the registry, an
  intentional `WI-PROMOTE-0097`-time design choice; that two call sites
  are technically registry-routable (genre v1 validation, and — with
  more restructuring — scenes.json) but neither achieves full literal
  compliance alone, since `is_legacy_flat_sidecar()`'s shape-detection
  call has no registry equivalent in either usage.
- Recommended narrowing the criterion wording rather than a partial code
  swap, and proposed exact replacement text for both
  `WS-PROMOTE-MODE-REDESIGN`'s exit criterion 3 and `WI-PROMOTE-0097`'s
  acceptance criterion (neither file edited, per this WI's Non-Goals).
- No code change applied (per the note's own reasoning: the two
  routable swaps don't achieve full compliance on their own, and the
  scenes.json swap changes emitted error-message wording — deferred as
  optional future cleanup, documented in the note's Follow-up section).
- Diff-mode self-review (cold-context subagent + independent
  re-verification) found 1 real gap (acceptance criterion 1's required
  origin/PR-#405-impact narrative was in the WI's own Problem/Context but
  missing from the deliverable document) and 1 minor quoting nit; both
  fixed before push.
- Opened PR #427.

# Validation

- `lrh validate`: 0 errors.
- Diff-mode self-review: 1 real gap found and fixed, 1 minor nit fixed;
  independently re-verified both directly against the real file content.

# Follow-up

- `/lrh-land` chain still to run for PR #427 (review-response,
  confirm-fixes, merge, closeout).
- If the proposed wording change is accepted, whoever closes this WI (or
  a human reviewing the note) should apply it directly to
  `WS-PROMOTE-MODE-REDESIGN.md` and `WI-PROMOTE-0097.md` — not done here
  per this WI's Non-Goals.
- Optional future cleanup identified but not required: two of the four
  `genre_sidecar` call sites in `_validate_sidecars` could be routed
  through the registry (documented in the note's own Follow-up section).
