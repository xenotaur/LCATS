---
execution_id: 2026_08_29_16_31_10_WI_GATHER_0101
prompt_id: PROMPT(WI-GATHER-0101:WI_GATHER_0101)[2026-08-29T15:55:49+00:00]
work_item: WI-GATHER-0101
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/414
commit: d0e7d69e2610053d456703379e6495c0cc3fddd7
created_at: 2026-08-29T16:31:10+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-GATHER-0101.md
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-execute WI-GATHER-0101` (inlined `/lrh-implement`) — audited
`mass_quantities`, `sherlock`, and `lovecraft`'s own separate `gather()`
implementations against `gatherlib.gather()`'s real signature and
behavior, and wrote findings to a new
`project/design/gatherer-reconciliation-audit.md`, following the
`WI-SEGMENT-0069`/`WI-EVENT-0028` precedent for investigation-type work
items.

# Result

Read `gatherlib.gather()`, `gatherlib.find_paragraphs`,
`gatherlib.create_download_callback`, and `downloaders.DataGatherer.download()`
in full to establish the real contract before auditing any of the 3
target sites, rather than relying on this session's earlier
(partially-corrected) reads.

**`sherlock`** — classified **full reconciliation**. Its `gather()`
reproduces `gatherlib.gather()`'s loop 1:1; the only differences are
values that would become call-site arguments (hardcoded
author/year instead of parameters). One real behavioral divergence
found and flagged, not glossed over: `find_paragraphs_adventures` only
collects `<p>` tags, while `gatherlib.find_paragraphs` also collects
`<pre>` tags — a genuine (likely-benign but unconfirmed) behavior
change if reconciled directly. Included a concrete, signature-correct
design sketch.

**`lovecraft`** — classified **no reconciliation** without extending
`gatherlib.gather()` itself first: each story has its own URL via a
per-entry `Extractor` object (not one shared `gutenberg_url`), and
extraction uses an ID-based mechanism (`extract_text_between_ids`) with
a fundamentally different signature than the heading-text-search
`paragraph_finder` contract `gatherlib.gather()` assumes. This corrects
the WI's own initial "near-identical" premise (already caught once by a
Codex review on the creation PR, and re-verified here rather than
assumed fixed).

**`mass_quantities`** — classified **no reconciliation**: structurally a
bulk Gutenberg-ID scanner with metadata-based filtering, not a
corpus-with-known-headings gatherer at all — no equivalent concept
exists in `gatherlib.gather()`'s model. Also corrected the WI's own
initial error-handling premise, verified line by line against
`parser.gather_story()` (`parser.py:1365-1483`): only `api.load_etext()`
is wrapped in `try`/`except`; metadata access, body processing,
filename construction, normalization, directory creation, and the final
JSON write are all unprotected and can propagate. `failed_stories`
mostly reflects deliberate early-return rejections, not caught
exceptions — in the one dimension that matters for the run-log
question, `mass_quantities` behaves the same as `gatherlib.gather()`
today (no isolation, whole-call abort on an uncaught exception).

No implementation was performed — per the WI's own Non-Goals, none of
the 3 findings was unambiguous enough to act on without a live human
decision; all three are reported as findings for follow-up work items,
with a recommendation table in the doc itself.

Ran a diff-mode `/lrh-self-review` pass before pushing (Step 7.5) —
clean, no findings; the subagent was explicitly told this WI's own
creation PR (#412) already had two premise corrections caught by review,
and to re-verify every claim rather than assume this doc already got it
right. It independently re-verified all citations, including the
highest-stakes `mass_quantities` claim line by line. I independently
re-verified that same claim myself via a direct `grep` against
`parser.py`.

Opened PR #414 (`xenotaur/spike/wi-gather-0101`) against `main`.

# Validation

- `lrh validate` — exit 0; no new warnings attributable to the new
  design doc.
- Diff-mode `/lrh-self-review` — subagent independently verified every
  file:line citation in the doc against the real code; I independently
  re-verified the single highest-stakes claim (mass_quantities'
  exception scope) myself via `grep`.

# Follow-up

- Reminder: `session_transcript` should be confirmed/updated at closeout
  time if it differs from the live `CLAUDE_CODE_HOST_SESSION_ID`
  convention.
- Next: wait for reviewer comments,
  `/lrh-review-response`/`/lrh-confirm-fixes` on PR #414, then merge.
- Per the design doc's own recommendation table: `sherlock` reconciliation
  is a good candidate for a small follow-up deliverable WI once someone
  confirms the `<pre>`-tag question; `lovecraft` reconciliation would
  first need `gatherlib.gather()` itself deliberately widened as its own
  separate WI; `mass_quantities` needs its own dedicated run-log work
  item if coverage is wanted there, not a `gatherlib.gather()` change.
  None of these follow-ups were created by this WI, per its own scope.
