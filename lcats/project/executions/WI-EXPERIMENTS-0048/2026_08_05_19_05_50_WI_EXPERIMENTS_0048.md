---
execution_id: 2026_08_05_19_05_50_WI_EXPERIMENTS_0048
prompt_id: PROMPT(WI-EXPERIMENTS-0048:WI_EXPERIMENTS_0048)[2026-08-05T19:05:35+00:00]
work_item: WI-EXPERIMENTS-0048
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/225
commit: cf5985f127e839cd59de04b4b24ac92b02d96012
created_at: 2026-08-05T19:05:50+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-EXPERIMENTS-0048.md
session_transcript: claude-app:beb4f32f-e43f-4fd8-a6cf-f9ad224728a1
---

# Summary

Implementation of `WI-EXPERIMENTS-0048` via `/lrh-execute`'s inlined
`/lrh-implement`: fix hardcoded flat-layout paths in the two
extract-scenes/clean-corpus notebooks.

# Result

- All 4 Required Changes implemented via `NotebookEdit`:
  `12_extract_scenes.ipynb` gained a `discovery` import, a new
  `canonical_story_files` variable, and `SAMPLE_OF_10`/`SAMPLE_OF_100`
  rewritten as seeded `random.sample()` calls;
  `13_clean_corpus.ipynb`'s `missing_stories` repointed at the real
  bucket directories.
- Mid-implementation, `origin/main` moved (concurrent, unrelated PR
  #223) -- rebased onto the new tip before pushing so the PR diff would
  only show this WI's own changes.
- Caught and fixed a `NotebookEdit` tooling artifact (both notebooks
  lost their trailing newline) and two pinned-tool version-skew issues
  (black/ruff) during validation, reinstalling the pinned versions
  rather than reformatting to the newer tools' opinions.
- Ran `/lrh-self-review` in diff-mode (Step 7.5) before the first push:
  cold-context subagent found no issues, confirmed all 4 Required
  Changes and all Non-Goals respected via a programmatic cell-level
  diff audit; independently re-verified that the untouched cells'
  IDs never appear in the diff at all. Record:
  `project/executions/AD_HOC/2026_08_05_19_03_54_WI_EXPERIMENTS_0048_SELFREVIEW.md`.
- Opened [PR #225](https://github.com/xenotaur/LCATS/pull/225).

# Validation

- Both notebooks confirmed to still parse as valid JSON.
- `scripts/format --check --diff`, `scripts/lint` -- clean (after
  reinstalling pinned black 25.11.0 / ruff 0.15.0).
- `scripts/test` (full suite) -- 1593 passed.
- `lrh validate` -- 0 errors, 72 pre-existing warnings.
- Manual: confirmed `corpora/mass_quantities/george_walker_at_suez__trollope`
  and `.../give_back_a_world__gallun` exist on disk with `story.json`.

# Follow-up

- `status` is `in_progress`; update via `/lrh-closeout` once PR #225
  lands (per `/lrh-execute` Step 4, inlining `/lrh-land`'s Steps 1-8).
- Once merged, mark the corresponding `project/design/backlog.md` entry
  resolved -- this is the last item from the original resolution plan.
