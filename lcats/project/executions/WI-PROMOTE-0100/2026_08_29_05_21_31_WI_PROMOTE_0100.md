---
execution_id: 2026_08_29_05_21_31_WI_PROMOTE_0100
prompt_id: PROMPT(WI-PROMOTE-0100:WI_PROMOTE_0100)[2026-08-28T20:43:23+00:00]
work_item: WI-PROMOTE-0100
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/411
commit: fafcb3d4297fa79ed4c64786fb87c1f6b81e0eb7
agent: claude_app
instruction_source: project/work_items/proposed/WI-PROMOTE-0100.md
session_transcript: claude-app:6a2dbae2-adca-4a2a-92fe-2e95d3b2a4e0
created_at: 2026-08-29T05:21:31+00:00
---

# Summary

Implemented WI-PROMOTE-0100 (Stage 3 of `WS-PROMOTE-MODE-REDESIGN`):
added an optional live-directory-scan sourcing mode to `lcats promote
insert`/`upsert`, alongside the existing `--tranche-manifest` mode.

# Result

- `src/lcats/analysis/corpus/promote.py`: refactored
  `_promote_sidecar_manifest()` into a shared `_promote_sidecar_records()`
  engine that consumes `(label, envelope, error)` triples from either
  `_iter_manifest_records()` (unchanged manifest-file behavior) or the
  new `_iter_scanned_sidecar_envelopes()` (scans
  `<source_root>/<collection>/<story>/<sidecar_filename>` for every
  story bucket found via `discovery.iter_collection_story_files()`,
  building `{"lcats_id": <bucket-relative-path>, "payload": <parsed
  sidecar JSON>}` for every bucket that already has the sidecar file
  present; buckets without it are silently skipped, not reported).
  Both sources share identical validation, escape-check,
  identity-agreement, unsafe-filename-guard, legacy-flat-genre refusal,
  and write-failure-as-per-record-rejection logic — no second
  promotion code path. `promote_sidecar_insert()`/`upsert()` gained a
  new keyword-only `scan_source` parameter; exactly one of
  `manifest_path`/`scan_source` must be given, enforced by a new
  `_resolve_sidecar_records()` helper (`ValueError` otherwise).
- `src/lcats/analysis/corpus/promote_cli.py`: added `--source` to
  `insert`/`upsert`, in an `argparse` mutually-exclusive group with the
  existing `--tranche-manifest` (`required=True` on the group, not on
  either individual argument).
- `tests/analysis_tests/promote_test.py`: added
  `PromoteSidecarScanSourcingTest` (discovery across multiple
  collections, skip-without-sidecar, identity-mismatch rejection,
  missing-destination-bucket rejection, invalid-payload rejection,
  malformed-JSON-is-per-record-not-fatal, upsert-overwrite,
  insert-refuse-on-conflict, mutual-exclusivity at the library level,
  dry-run, unsafe-filename-guard) plus CLI-level tests for `--source`
  and its mutual exclusivity with `--tranche-manifest`.
- `docs/reference/corpus-promotion.md`, `docs/reference/cli-commands.md`:
  documented the new `--source` scan-sourcing mode.
- Diff-mode self-review (cold-context subagent) found no issues;
  independently re-verified its two most load-bearing claims directly
  (safety-guard ordering before the record loop, CLI mutual-exclusivity
  composition) before pushing.

# Validation

- `scripts/version tools`: ruff 0.15.0, black 25.11.0 (matches pins;
  re-pinned once mid-session after recurring shared-worktree drift).
- `scripts/format --check --diff`: 2 files needed reformatting
  (`promote.py`, `promote_test.py`); applied via `scripts/format`,
  re-verified clean.
- `scripts/lint`: clean.
- `scripts/test`: 2237 tests, `OK`.
- `lrh validate`: 0 new errors.
- Diff-mode self-review: no findings; independently re-verified.

# Follow-up

- Stage 2 (`replace`'s orphaned-sidecar guard,
  `--allow-orphaned-sidecar-deletion`) remains unminted, independent of
  this item.
