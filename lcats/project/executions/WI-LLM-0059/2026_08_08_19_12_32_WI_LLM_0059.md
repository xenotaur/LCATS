---
execution_id: 2026_08_08_19_12_32_WI_LLM_0059
prompt_id: PROMPT(WI-LLM-0059:WI_LLM_0059)[2026-08-08T18:21:30+00:00]
work_item: WI-LLM-0059
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/266
commit: 
created_at: 2026-08-08T19:12:32+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0059.md
session_transcript: pending
---

# Summary

Implemented `WI-LLM-0059`: tested whether appending WI-LLM-0051's
system-prompt reminder to the real production `SCENE_SEQUEL_SYSTEM_PROMPT`
(not just `common/harness.py`'s harness-scoped retry copy) improves
local-model segmentation reliability without regressing the frontier
paths (Anthropic, OpenAI) that prompt is shared with.

# Result

Ran real, live calls across all three required paths via two new scripts
(`experimental/model_comparison/wi_llm_0059/run_local_reminder.py`,
`run_frontier_paired.py`) that call `common/harness._run_segmentation_once()`
directly with the reminder appended eagerly (not just on retry):

- **Local (Ollama):** 4 calls (`qwen3:8b` x3, `qwen3:30b-a3b` x1 - first
  test for that model), 0/4 succeeded. The 3 `qwen3:8b` calls pool with
  WI-LLM-0051's prior 5-call `qwen3:8b` sample (identical mechanism):
  2/8 (25%) combined, consistent with WI-LLM-0051's original 40%
  estimate.
- **Anthropic (`claude-opus-4-8`):** 3 paired baseline/modified real
  calls (not a single pair, per the P1 review finding on WI-LLM-0059's
  own planning PR #260) - 3/3 success both conditions, comparable
  latency, no systematic segment-count shift.
- **OpenAI (`gpt-4o`):** untested - real API key present but the
  organization has zero remaining credits; both calls in the one
  attempted pair failed identically with `429 insufficient_quota`.

**Verdict: `SCENE_SEQUEL_SYSTEM_PROMPT` was NOT edited.** Per
`WI-LLM-0059`'s own Required Changes item 5, an untested OpenAI path
forces the documented no-change outcome regardless of the other two
results. Documented in `PROP-ERW-LOCAL-MODEL-EVALUATION`'s new "Decision
3 update (2026-08-08, production system-prompt reminder, WI-LLM-0059)"
section and its Open Questions.

A diff-mode `/lrh-self-review` pass (cold subagent, before this PR's
first push) caught two real issues in the first draft, both fixed before
push: an arithmetic error ("2/9 combined, all qwen3:8b" when one of the
4 new calls was actually `qwen3:30b-a3b` - corrected to 2/8 for
`qwen3:8b` specifically), and an unsupported "prior sessions" variance
comparison for `anthropic_opus` segmentation data that doesn't exist in
the repo (this PR's 3 pairs are the first `anthropic_opus`
segmentation-stage data recorded at all).

Opened PR #266 with the two new scripts, their real result JSON files, a
new `wi_llm_0059/README.md`, and the proposal update.

# Validation

- `./scripts/version tools` - `ruff 0.15.0`, `black 25.11.0` (CI pins
  matched exactly).
- `./scripts/format --check --diff` - clean (repo-scoped; does not cover
  `lcats/experimental/`, per prior-session convention - ran `black`/`ruff`
  directly on the two new files too, both clean after a `black`
  reformat).
- `./scripts/test` - 1664 passed.
- `lrh validate` - 0 errors, 123 warnings (all pre-existing).
- Real benchmark calls: 4 Ollama (free), 6 Anthropic (billed), 2 OpenAI
  (billed, both failed on quota) - all real, not simulated, committed as
  `results_local_reminder_eager.json` and `results_frontier_paired.json`.
- `git diff -- src/lcats/analysis/scene_analysis.py` against `main` -
  empty, confirming the "do not edit" verdict was genuinely honored in
  code, not just claimed in prose (independently re-verified by the
  diff-mode self-review subagent too).

# Follow-up

- Re-running just `run_frontier_paired.py`'s OpenAI leg once real API
  credits are available could revisit this verdict without repeating the
  Anthropic or local legs.
- `session_transcript: pending` above should be updated to
  `claude-app:<host-uuid-stem>` after this session ends.
