---
execution_id: 2026_08_20_22_44_23_WI_SEGMENT_0070
prompt_id: PROMPT(WI-SEGMENT-0070:WI_SEGMENT_0070)[2026-08-20T22:23:59+00:00]
work_item: WI-SEGMENT-0070
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/324
commit: 8d7dd28bf0eb909d28c1263a30a16e0cf5fa360b
created_at: 2026-08-20T22:44:23+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-SEGMENT-0070.md
session_transcript: claude-app:693d6013-727b-422d-a378-5dc4242d3076
---

# Summary

Implemented `WI-SEGMENT-0070`: fixed the two concrete segmentation
alignment failure sub-patterns (paragraph-index-marker leakage,
Unicode quote/dash typography mismatch) identified in
`WI-SEGMENT-0069`'s investigation.

# Result

- Required Change 1: `_locate_anchor_span` strips a leaked
  `\[P\d{4}\]\s*` paragraph-index marker (`add_paragraph_markers`'
  exact 4-digit zero-padded format, not a looser `\d+`) from the anchor
  before building the whitespace-tolerant regex, handling both a
  leading marker and a mid-anchor marker at a segment's own paragraph
  boundary.
- Required Change 2: `_locate_anchor_span` normalizes Unicode curly
  quotes (`“”‘’`) and em/en dashes to ASCII equivalents on both the
  anchor and the searched text, via a length-preserving `str.translate`
  so a match found in the normalized copy still maps 1:1 onto the real
  position in the original text.
- Required Change 3: added `TestWiSegment0070MarkerAndTypographyReplay`
  to `tests/analysis_tests/text_segmenter_test.py`, replaying all 16
  real anchors from `WI-SEGMENT-0069`'s committed fixture
  (`experiments/03_cross_segment_relation_pilot/fixtures/wi_segment_0069_alignment_cases.json`)
  end-to-end via `align_segment`, plus synthetic edge cases for a
  mid-anchor marker and a non-marker 3-digit bracket that must not be
  stripped.
- Required Change 4: ran a targeted 5-story live smoke test (real API,
  the exact cited stories, sample-size authorized in-session) via
  `--story-list`. 2 stories (the marker-leakage cases) now align
  successfully. The other 3 hit genuinely different, out-of-scope
  failure modes (1 paragraph mis-numbering, 2 near-miss quoting) --
  confirmed via `classify_alignment_failures.py` and a direct scan of
  every segment's raw anchors in `parsed_output` for a leaked marker or
  curly-quote/dash mismatch, finding none. Zero marker-leakage or
  typography-mismatch `alignment_error` outcomes occurred in this run.
- Required Change 5: updated `lcats/project/design/backlog.md`,
  mirroring `WI-SEGMENT-0068`'s own convention.
- Opened [PR #324](https://github.com/xenotaur/LCATS/pull/324).

# Validation

- `python -m pytest tests/analysis_tests/text_segmenter_test.py` -- 86
  passed (81 pre-existing + 5 new).
- `./scripts/test` (full suite, via the pinned `LCATS` conda env) --
  1767 passed.
- `black --check` / `ruff check` (pinned `LCATS` conda env, matching
  CI's exact pins -- the base conda env's black/ruff had drifted ahead
  of the pins by the time this ran) -- clean.
- `lrh validate` -- 0 errors, 157 warnings (pre-existing class,
  unrelated).
- Live 5-story smoke-test re-run against `claude-haiku-4-5-20251001` --
  0 marker-leakage/typography-mismatch failures observed.

# Follow-up

- This record documents implementation; `status` stays `in_progress`
  until `/lrh-land` merges PR #324 and lands closeout.
- Paragraph mis-numbering and the near-miss-quoting bucket remain
  explicitly out of scope, per this WI's own Non-Goals -- no follow-up
  WI created for either yet.
