---
execution_id: 2026_09_02_19_36_53_WI_GATHER_0104
prompt_id: PROMPT(WI-GATHER-0104:WI_GATHER_0104)[2026-09-02T19:26:35+00:00]
work_item: WI-GATHER-0104
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/424
commit: 
created_at: 2026-09-02T19:36:53+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-GATHER-0104.md
session_transcript: claude-app:7065c30d-504e-47af-9834-d062b53d7a74
---

# Summary

`/lrh-execute WI-GATHER-0104` (inlined `/lrh-implement`) — extended
`gatherlib.gather()` with 3 opt-in extensions and reconciled
`lovecraft/gatherer.py` onto it, per `WI-GATHER-0101`'s audit.

# Result

Added `entry_url`, `extraction_strategy`, `name_source` as opt-in
parameters to `gatherlib.gather()` (and `extraction_strategy` to
`create_download_callback()`), each defaulting to `None`/prior behavior.
Migrated `lovecraft/gatherer.py`'s `gather()` to call the extended
function, wiring per-entry `Extractor` data through three small helper
functions (`_entry_url`, `_entry_name`, `_extract_story_text`) that key
off a `_EXTRACTOR_BY_FILE` lookup dict. Removed the now-dead
`create_download_callback` and its `BeautifulSoup`/`downloaders`
imports. Set `verbose=False` in lovecraft's `gather()` call, matching
`WI-GATHER-0103`'s established fix (avoids duplicate console output with
`main()`'s own prints — not explicitly required by this WI's acceptance
criteria but the same known issue category).

Extended `gatherlib_test.py` with a new `TestGatherExtensionPoints`
class (4 tests) covering each extension point independently, including
a test that omitting all three reproduces the original behavior exactly.
Retargeted `lovecraft_gatherer_test.py`'s `TestGather` to mock
`gatherlib.downloaders.DataGatherer` (the true network boundary,
matching `WI-GATHER-0103`'s established pattern and `AGENTS.md`'s
mocking philosophy) instead of the retired `create_download_callback`,
plus a `TestEntryHelpers` class (3 tests) and an end-to-end wiring test
that captures the real callback and invokes it with real HTML.

Confirmed via a smoke test that `gather()` now produces
`logs/gather/lovecraft_gather_run_log.jsonl` — `lovecraft` inherits
`gatherlib.gather()`'s existing `RunLog` coverage, closing the
`WI-RUNLOG-0082` gap for this site.

Ran a diff-mode `/lrh-self-review` pass before pushing (Step 7.5) —
clean, no findings; the subagent independently re-read every other
`gatherlib.gather()` caller and confirmed backward compatibility, and
ran the affected tests directly. I independently re-verified that claim
myself via direct greps of two callers' own call sites.

Opened PR #424 (`xenotaur/feat/wi-gather-0104`) against `main`.

# Validation

- `lrh validate` — exit 0.
- Full test suite (`PYTHONPATH`-corrected) — 2242/2242 pass.
- `scripts/format --check --diff` / `scripts/lint` — clean (one
  `scripts/format` auto-fix applied for line-length wraps).
- Diff-mode `/lrh-self-review` — clean, no findings; top claim
  independently re-verified.

# Follow-up

- Next: wait for reviewer comments,
  `/lrh-review-response`/`/lrh-confirm-fixes` on PR #424, then merge.
