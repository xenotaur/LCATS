---
execution_id: 2026_09_25_07_35_24_WI_TEST_0107
prompt_id: PROMPT(WI-TEST-0107:WI_TEST_0107)[2026-09-25T07:11:10+00:00]
work_item: WI-TEST-0107
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/447
commit: bfdd60824e0e7907e512be43a338a8bf18437469
session_transcript: codex-app:01a0c794-1b86-77a2-a36a-7d9393962f62
created_at: 2026-09-25T07:35:24+00:00
---

# Summary

Execute WI-TEST-0107 by surveying `scripts/test` output and suppressing
unwanted test-body output at individual call sites with the existing LCATS
capture utilities, while preserving intentional output assertions and the
unittest dot/F/E signal.

# Result

The baseline run passed 2,342 tests and exposed these in-scope sources:

- JSON assessment output from `tests/analysis_tests/assess_cli_test.py`.
- Discovery warnings from `tests/analysis_tests/discovery_test.py`.
- Corpus processing messages/progress from `tests/analysis_tests/processing_test.py`.
- Corpus-stat progress from `tests/analysis_tests/corpus_cli_test.py`.
- Stanza initialization logs from `tests/analysis_tests/event_role_world_test.py`.
- Parser fallback diagnostics from `tests/gatherers_tests/parser_test.py`.
- Child-process `git push` output from `tests/dev_tests/versioning_test.py`.
- Run-log failure diagnostics from `tests/utils_tests/run_log_test.py` and
  experiment validation tests in `experiments/05_metadata_genre_prefilter/`.

The implementation applies `capture.suppress_output()` locally, adds an
opt-in file-descriptor mode for child processes and preconfigured logging
streams, and adds coverage for that mode. The review follow-up also moved
descriptor setup inside the context's cleanup boundary and added a pipe-based
test proving that descriptors are restored after suppression. Tests that
inspect output continue to use `capture.capture_output()`.

The first post-review validation attempt in the restricted sandbox reached
the corpus-stat tests but failed three tests because `tiktoken` could not
resolve its uncached encoding from `openaipublic.blob.core.windows.net`.
With the approved network-enabled execution, the final run passed 2,344
tests and retained normal unittest progress. The only remaining non-dot text
in this sandbox was Matplotlib/Fontconfig cache diagnostics emitted during
third-party import because the sandbox home cache is unwritable; this is
environment output, not LCATS test-body printing.

# Validation

- Targeted regression tests: 249 passed, including six capture utility tests
  after the review hardening.
- Restricted-sandbox `scripts/test`: 2,344 tests reached, with three
  environment-only tiktoken download errors.
- Network-enabled `scripts/test`: 2,344 tests, OK.
- `scripts/format --check --diff`: passed with pinned Anaconda tools.
- `scripts/lint`: passed with pinned Anaconda tools.
- `lrh validate`: 0 errors (330 pre-existing warnings).
- Hosted PR CI: coverage, lint, and both test jobs passed.
- Review follow-up: descriptor restoration is failure-safe and covered by a
  direct file-descriptor regression test.
- Diff-mode cold-context self-review completed; its P1 was rejected on direct
  re-verification because reopening the WI was explicitly requested, and its
  P2 evidence observation is addressed by this record.

# Follow-up

Land PR #447, commit the execution record, resolve WI-TEST-0107 during
closeout, and record the final merge commit and session transcript pointer.
